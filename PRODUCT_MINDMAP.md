---
markmap:
  initialExpandLevel: 2
  colorFreezeLevel: 2
---

# 软件

## 设备配置与连接

### 目标

- 识别合法设备
- 建立稳定串口连接
- 提供实时数据入口

### 实现逻辑

- 检查本地设备配置
- 扫描串口并筛选设备
- 探测候选波特率
- 读取 MAC 并匹配设备类型
- 校验授权和矩阵尺寸
- 绑定数据监听并推送状态

## 设备数据接收与处理

### 目标

- 原始帧转标准压力矩阵
- 统一展示、统计、采集、导出数据源
- 保留全量矩阵和关键元数据

### 实现逻辑

- 接收串口原始帧
- 校验帧头、长度、完整性和点数
- 丢弃坏帧并记录数据质量
- 按设备类型映射矩阵
- 按方向生成业务矩阵
- 计算采样率并生成标准数据
- WebSocket 分发给下游模块

## 展示视图

### 目标

- 实时展示压力变化
- 支持坐垫、靠背、整体视图
- 显示设备在线、离线和异常状态

### 实现逻辑

- 接收实时帧和回放帧
- 按显示对象选择矩阵
- 应用方向和可视化配置
- 渲染 2D 数字视图
- 渲染 3D 点图或模型
- 切换视图时重新映射数据

## 功能工具模块

### 目标

- 提供现场分析工具
- 支持方向修正和局部分析
- 不破坏原始数据和历史数据

### 实现逻辑

- 置零记录压力基线
- 翻转和旋转修正业务矩阵
- 框选保存区域坐标
- 多框选独立计算
- 模板复用区域配置
- 量尺按点距换算距离
- 可视化调节只影响展示

## 数据可视化与计算指标

### 目标

- 生成统计指标
- 生成压力、面积、重心趋势图
- 支持全量和框选区域统计

### 实现逻辑

- 判断统计范围
- 应用方向、零点和过滤阈值
- 计算平均压力
- 计算最大压力
- 计算非零最小压力
- 计算压力总和
- 计算有效点数和受压面积
- 计算压力重心
- 最近 30 帧滚动展示

## 采集、历史数据与回放

### 目标

- 保存采集过程
- 生成可回放历史记录
- 支持异常恢复和复核

### 实现逻辑

- 采集前再次校验设备
- 创建采集任务
- 持续保存全量矩阵
- 同步保存方向、零点、采样率等元数据
- 停止采集后生成历史记录
- 回放时逐帧推送前端
- 倍速通过帧间隔控制

## 数据导出与导入

### 目标

- 历史数据可交付
- CSV 可复核、可导入、可再分析
- 兼容旧版本数据

### 实现逻辑

- 读取历史矩阵和元数据
- 生成 CSV 文件
- 写入方向和统计字段
- 附带框选区域信息
- 导入时校验字段结构
- 校验矩阵点数和数值合法性
- 通过后进入回放和分析流程

## 数据对比

### 目标

- 比较两条历史记录
- 支持 A/B 方案判断
- 输出差值和变化率

### 实现逻辑

- 选择基准 A 和对比 B
- 校验记录不可相同
- 校验设备类型和矩阵尺寸
- 按进度同步两组帧
- 计算 B 减 A 差值矩阵
- 计算当前帧指标和变化率
- 框选区域同步对比

## 全局数据原则

### 目标

- 统一数据口径
- 保证可回放、可复核、可对比

### 实现逻辑

- 原始帧用于排查
- 业务矩阵作为统一基准
- 方向修正写入元数据
- 框选不裁剪采集数据
- 展示配置不污染原始矩阵
- 噪点处理写入元数据
- CSV 保留关键字段
