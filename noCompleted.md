# 功能完成度跟踪

> 维护规则：未完成项放在每个分类最上面；每次按 PRD 或代码实现完成新能力后，同步更新状态、完成度、未完成原因和下一步。  
> 原因口径：`开发未完成` 表示 PRD 已有方向但代码未落地；`PRD 未描述清楚` 表示需求边界、交互、字段或验收标准不够明确；`需验证` 表示已有实现但还需要实机或异常场景回归；`已完成` 表示当前实现基本满足需求。

## 连接与设备

| 状态 | 功能 | 完成度 | 未完成原因 | 下一步 |
|---|---|---:|---|---|
| 未完成 | `/startCol` 授权、MAC、设备类型强校验 | 60% | 开发未完成 | 已有类型匹配拦截；继续补非法设备、MAC 未配置、授权失败的统一拦截和提示。 |
| 未完成 | 连接全流程错误码和用户提示统一 | 65% | 开发未完成 + PRD 未描述清楚 | 一键连接/重连已有基础状态；需要先统一 PRD 错误码、提示文案和日志口径，再补代码。 |
| 部分完成 | 一键连接与重新连接 | 80% | 需验证 | 支持扫描、连接、重连、状态反馈；继续验证连接锁释放、设备断开、异常重连等极端场景。 |
| 已完成 | MAC 配置强校验 | 95% | 已完成 | 前后端已拦截格式错误、重复 MAC/Unique ID、不支持的设备类型；批量保存先整体验证再写入。 |

## 数据处理

| 状态 | 功能 | 完成度 | 未完成原因 | 下一步 |
|---|---|---:|---|---|
| 部分完成 | 坏帧、断帧、长度不匹配阈值与提示 | 85% | 需验证 | 已补异常帧计数、1 秒坏帧率、连续 10 帧异常、矩阵尺寸错误、采样率异常提示和异常帧采集跳过；需实机回归阈值是否合适。 |
| 部分完成 | 完整数据字典 | 75% | 开发未完成 | 实时 payload 已补 `rawFrame/rawPointArray/matrixMeta/dataQuality/sampleRateHz`；还需补面向用户的正式字段说明文档。 |
| 部分完成 | 数据层隔离 | 90% | 需验证 | 框选不丢全量、显示方向和采集方向已有隔离；异常帧不入库和置零采集/导出 metadata 已补，滤波对导出的完整标记还需继续核对。 |
| 部分完成 | carY 分压规则说明 | 70% | PRD 未描述清楚 | 代码已有分压处理；还需要面向用户、导出字段和验收标准补充解释。 |

## 工具与方向

| 状态 | 功能 | 完成度 | 未完成原因 | 下一步 |
|---|---|---:|---|---|
| 部分完成 | 坐垫/靠背独立方向 | 88% | 需验证 | 前端方向状态支持 `byKey + rotateDegree`，采集和导出按矩阵 key 记录；还需要补 UI 当前方向状态展示。 |
| 已完成 | 0/90/180/270 旋转 | 90% | 需验证 | 新增顺时针 90° 旋转入口，按 `0/90/180/270` 累加，写入本地方向状态、采集和 CSV metadata。 |
| 已完成 | 多量尺上限 8 条和测距规则 | 90% | 需验证 | 量尺按当前设备点距计算直线距离，默认 cm 显示，最多 8 条，超限提示。 |
| 已完成 | 方向持久化 | 95% | 已完成 | 翻转和 90° 旋转方向写入 `localStorage`，刷新后恢复。 |
| 已完成 | 采集中禁止翻转/旋转 | 95% | 已完成 | 采集中上下/左右翻转和 90° 旋转会被拦截并提示停止采集后再修改。 |
| 已完成 | 预压力置零采集/导出口径 | 90% | 需验证 | 置零基线同步到后端，采集保存置零后的业务矩阵，CSV 导出 `zero_enabled/zero_time/zero_state`。 |
| 已完成 | 框选与采集解耦 | 95% | 已完成 | 采集保存全量矩阵，框选作为统计/展示元数据使用。 |

## 展示与回放

| 状态 | 功能 | 完成度 | 未完成原因 | 下一步 |
|---|---|---:|---|---|
| 部分完成 | 回放损坏帧跳过与提示 | 85% | 需验证 | 回放会校验空帧、矩阵长度和非法数值，能跳过异常帧；连续 10 帧异常会停止并提示，仍需用损坏历史样例回归。 |
| 部分完成 | 2D 放大镜 | 85% | 需验证 | 已支持 2D 数字视图；需要继续回归靠背/坐垫不同矩阵、缩放和边界显示。 |
| 已完成 | 连接后视图缩放范围 | 95% | 已完成 | 3D 点图/模型按钮和滚轮缩放主链路统一限制在 10%-300%，只影响当前显示视图，不影响采集、统计和导出。 |
| 已完成 | 3D 点图颜色上限映射 | 90% | 需验证 | 颜色上限只用于颜色饱和，超过上限按最大颜色显示；低于过滤阈值按无效点隐藏，不改变原始数据、统计、采集、回放和 CSV。 |
| 已完成 | 坐垫/靠背视图切换缺数据提示 | 90% | 需验证 | 连接后切换到坐垫或靠背 2D/3D 视图时会检查当前矩阵数据，缺少对应数据则提示并阻止切换。 |
| 已完成 | 回放图表显示 | 95% | 已完成 | 历史曲线不再依赖实时帧，回放加载后可显示压力/面积曲线。 |
| 已完成 | 2D 数字视觉恢复 | 95% | 已完成 | 固定 `40px/32px`、白色无描边、渲染前整数化。 |
| 已完成 | 3D 视角中心优化 | 90% | 需验证 | 靠背/整体切换后的旋转中心已优化；仍建议实机回归确认动画和视角一致。 |

## CSV 与历史数据

| 状态 | 功能 | 完成度 | 未完成原因 | 下一步 |
|---|---|---:|---|---|
| 未完成 | CSV legacy 字段映射增强 | 65% | 开发未完成 + PRD 未描述清楚 | 当前能校验系统导出 CSV；旧格式兼容范围、字段别名和缺省策略需要 PRD 明确后增强。 |
| 未完成 | 采集中强退后的恢复流程 | 40% | 开发未完成 + PRD 未描述清楚 | 需要明确恢复、丢弃、缓存保护和用户确认流程，再补代码实现。 |
| 部分完成 | 历史字段结构完整性 | 85% | 需验证 | 历史帧已记录方向、置零、矩阵、采样率、设备类型和部分 MAC 信息；固件版本、异常中断状态字段仍需和真实设备回归。 |
| 部分完成 | CSV 导出 metadata | 92% | 需验证 | 已补格式版本、软件版本、设备 MAC/类型、系统/矩阵、采样率、波特率、方向、旋转、置零、单位、导出时间和 carY 换算说明。 |
| 部分完成 | CSV 导入结构校验 | 80% | 需验证 | 已校验表头、矩阵长度、数值合法性，不合规提示“数据有误”；还需要补更多异常样例。 |
| 已完成 | 标准 `/download` 接口 | 95% | 已完成 | 新增 `/download`，旧 `/downlaod` 保留兼容。 |

## 数据对比

| 状态 | 功能 | 完成度 | 未完成原因 | 下一步 |
|---|---|---:|---|---|
| 已完成 | 对比结果导出 | 90% | 需验证 | 对比页新增“导出结果”，按当前对象和框选/全量范围导出 A、B、B-A 指标 CSV；仍需真实业务样例核对字段。 |
| 已完成 | 对比曲线 | 90% | 需验证 | 对比页新增压力变化曲线、面积变化曲线，展示 A、B、B-A 趋势；仍需长帧数历史样例回归性能和视觉密度。 |
| 部分完成 | 历史 A/B 对比 | 96% | 需验证 | 已有校验、A/差值/B 三栏、差值图内固定图例、当前帧结论、状态条、区域管理侧栏、全量/框选切换、导出预览、差值和指标；仍需更多真实样例回归。 |
| 部分完成 | 框选 Designer 交互反馈 | 96% | 需验证 | UX-01/02/03/05 已补：实时/回放 2D 复用历史数据同款右侧 Drawer、画布编号、颜色一致、拖拽/缩放尺寸与行列范围提示、选中态高亮、差值图内固定图例；仍需在不同矩阵尺寸和缩放比例下人工回归。 |
| 已完成 | 多抽屉点击置顶 | 98% | 需验证 | 公共 Drawer 通过 portal 挂到 document.body，并在打开或点击时自动提升 z-index，多个抽屉同时显示时最近点击的抽屉在最上层。 |
| 已完成 | 框选非画布误提示 | 95% | 需验证 | 框选模式下点击抽屉、工具栏、底部控制区、弹窗或普通页面区域会被忽略，只有点击 2D canvas 但超出真实矩阵区域才提示。 |
| 已完成 | 框选控制点样式 | 95% | 需验证 | 移除框内编号避免遮挡矩阵，右上角删除按钮改为统一关闭 iconfont 并独立外置，避免与右上缩放调节点重叠。 |
| 已完成 | 坐垫对坐垫、靠背对靠背校验 | 90% | 已完成 | 不可比时给出明确提示。 |
| 已完成 | 对比模式独占视图 | 90% | 已完成 | 对比模式下隐藏其他分析数据，避免页面拥挤。 |
