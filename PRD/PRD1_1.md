# 压力感知软件产品需求文档 PRD1.1（基于飞书表格版-含数据处理模块增补）

文档日期：2026-05-09  
产品名称：压力感知软件 JQ Tools  
版本范围：V2.0  
文档版本：V1.6  
文档用途：复制到飞书云文档，用于产品评审、研发拆解、QA 验收  
当前基线：已实现功能 + 原 PRD + 实现文档 + 优化标注
本版优化：合并 `dec.md` 中 DEC-12 至 DEC-18 的决策；明确采样率来源、3D 点图颜色上限、受压面积阈值、多框选曲线、实时图表 30 帧缓存、回放倍速；补充历史数据定义，DEC-19 清理策略继续待确认。

## 0. 版本更新记录

说明：版本更新记录按最新版本在上方排列。

| 文档版本 | 更新日期 | 更新人 | 更新内容 |
|---|---|---|---|
| V1.6 | 2026-05-11 | Codex | 合并 DEC-12 至 DEC-18 决策：采样率以后端帧时间计算为准并记录硬件返回值；foot/bigHand 矩阵尺寸本版不做最终化；3D 点图颜色映射支持用户直接设置上限值；受压面积有效阈值使用当前过滤值；多框选曲线按区域独立展示；实时图表滚动显示最近 30 帧；回放倍速固定为 0.5x、1x、2x、4x。补充历史数据定义，保留本地缓存清理规则为待确认项。 |
| V1.5 | 2026-05-11 | Codex | 修正数据层口径：翻转/旋转会修改业务 rawData 并影响展示、统计、采集、回放、导出；噪点消除不改 rawData，但会生成后端处理数据流用于后续展示和分析；CSV 数据区补充最大值坐标、框选统计字段、框选行区间和列区间；检查剩余待决策事项并追加到 `dec.md`。 |
| V1.4 | 2026-05-11 | Codex | 合并 `dec.md` 已确认和默认推荐决策：固件版本由硬件返回并由软件读取记录；压力值暂使用软件内部单位；MAC 批量录入暂沿用 `MAC地址:类型`；量尺点距由软件按设备类型配置维护；90 度旋转使用单按钮顺时针累加并记录累计角度；恢复默认只影响当前对象；CSV 导入兼容无版本号、旧字段别名和缺省 metadata；强退后默认推荐恢复为异常中断历史记录；对比导出先导出当前帧和框选指标；2D 压力云图暂不进入 V2.0；缩放范围确认为 10%-300%。 |
| V1.3 | 2026-05-11 | Codex | 根据 `noCompleted.md` 完善未完成项 PRD：补齐连接强校验、错误码落地、数据字典、异常帧阈值、置零/滤波数据流、回放损坏帧、CSV legacy、强退恢复、数据对比导出与曲线规则；待产品决策项单独写入 `dec.md`。 |
| V1.2 | 2026-05-09 | Codex | 确认当前无 2D 热力图；合并量尺与画布翻转细化规则，补充 90 度旋转、坐垫/靠背独立方向、方向持久化、采集中禁止翻转、多量尺上限 8 条。 |
| V1.1 | 2026-05-09 | Codex | 整合一键连接/重新连接异常处理、框选独立分析与框选模板、历史 A/B 数据对比 PRD；补充版本号和版本更新记录。 |
| V1.0 | 2026-05-09 | Codex | 创建含数据处理模块的主 PRD，形成连接、数据处理、展示、工具、数据统计、采集回放、导出对比七大模块。 |

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 产品名称 | 压力感知软件 JQ Tools |
| 当前版本 | V2.0 |
| 文档版本 | V1.6 |
| 文档类型 | 当前实现对齐版 PRD |
| 文档重点 | 补齐每个模块的业务逻辑、数据口径、异常规则和验收标准；整合连接异常、框选模板、数据对比模块 |
| 主要场景 | 汽车座椅/靠背压力感知、展厅演示、研发测试、科研采集 |
| 主要对象 | 产品、研发、QA、算法、硬件、交付、售后 |


### 1.1 PRD1.1 增补说明

| 项目 | 说明 |
|---|---|
| 增补来源 | `ARCHITECTURE.md` 更新日志、`PRD/PRD1_1_增量清单.md`、近期功能实现和问题修复记录 |
| 增补方式 | 保留原 PRD 的章节编号、模块结构、业务逻辑、待补齐项和验收标准，在对应模块下追加 PRD1.1 补充规则 |
| 增补重点 | 连接配置强校验、异常帧处理、endi 靠背 1024 线序、框选解耦、回放框选、可视化调节、CSV 单文件导入导出、数据对比新版 UI |
| 文档边界 | PRD1.1 是对 V1.0 的补充，不删除 V1.0 已定义的主流程和业务规则 |

## 2. 命名规则

后续文档统一使用中文命名。

| 类型 | 命名规则 | 示例 |
|---|---|---|
| 产品需求文档 | `产品名称 + 产品需求文档 + 版本/用途` | `压力感知软件产品需求文档-飞书表格版-含业务逻辑.md` |
| 实现说明 | `模块名称 + 实现说明` | `设备连接实现说明.md` |
| 数据字典 | `产品名称 + 数据字典` | `压力感知软件数据字典.md` |
| 验收清单 | `产品名称 + 验收清单` | `压力感知软件验收清单.md` |
| 测试用例 | `产品名称 + 测试用例` | `压力感知软件测试用例.md` |

说明：`JQ Tools` 是软件品牌名，文档标题中可以保留；其他文件名、章节名、模块名优先使用中文。

## 3. 产品目标

| 类型 | 目标 |
|---|---|
| 产品目标 | 完成一套可交付的压力感知桌面软件 |
| 用户闭环 | 打开软件、连接设备、查看压力图、使用分析工具、采集数据、回放历史、导出 CSV |
| 数据目标 | 串口原始帧、原始矩阵、显示数据、统计数据、采集数据、导出数据口径清楚 |
| 交付目标 | 支持 Windows/macOS 打包、本地离线使用、设备配置和现场排查 |
| 后续目标 | 为数据对比、报告生成、标定分析和高级算法打基础 |

## 4. 用户场景

| 用户角色 | 使用场景 | 业务目标 | 关键功能 |
|---|---|---|---|
| 汽车座椅研发工程师 | 座椅/靠背压力测试 | 找出压力集中、支撑不足、左右不均衡等问题 | 2D/3D 视图、统计指标、采集、回放、CSV |
| 展厅/门店演示人员 | 现场演示压力变化 | 让客户直观看到坐上座椅后的压力分布 | 2D 数字视图、3D 模型、一键连接、稳定展示 |
| 科研客户 | 实验采集和数据分析 | 获取可追溯、可导出的实验数据 | 原始矩阵、采样率、历史数据、CSV |
| 交付/售后人员 | 安装、调试、排查 | 快速确认设备是否可用、数据是否正常 | MAC 配置、设备状态、错误提示、日志 |
| 内部 QA | 验证版本可发布 | 确认功能、数据、异常、性能满足准入 | 测试用例、验收清单、字段口径 |

## 5. 主流程业务逻辑

| 步骤 | 用户动作 | 前置条件 | 系统处理 | 成功结果 | 失败/异常处理 |
|---|---|---|---|---|---|
| 1 | 打开软件 | 软件已安装 | 检查本地设备配置 | 有配置进入主界面 | 无配置进入 MAC 配置页 |
| 2 | 录入设备配置 | 首次使用或设备未配置 | 批量保存 MAC 和设备类型 | 配置写入本地缓存 | 格式错误提示用户修改 |
| 3 | 点击一键连接 | 设备已插入 | 扫描串口、识别 CH340、探测波特率 | 建立串口连接 | 未找到设备或波特率失败时提示 |
| 4 | 读取设备信息 | 串口连接成功 | 读取 MAC、识别设备类型、读取采样率 | 显示设备在线和参数 | MAC 读取失败时禁止采集 |
| 5 | 授权/类型校验 | 已读取设备信息 | 校验设备是否允许采集、类型是否匹配当前系统 | 进入实时展示 | 校验失败时禁止采集 |
| 6 | 接收设备数据 | 设备在线且持续推送 | 接收串口原始帧、校验帧完整性、解析点阵 | 生成原始压力矩阵 | 断帧、坏帧、长度不匹配时丢弃并记录 |
| 7 | 处理压力矩阵 | 原始帧解析成功 | 按设备类型转换矩阵、计算采样率、生成推送数据 | 生成可供展示和统计的数据对象 | 矩阵尺寸不匹配时提示设备或配置异常 |
| 8 | 查看实时压力图 | 数据处理成功 | WebSocket 推送压力矩阵，前端渲染 | 2D/3D 视图实时变化 | 数据中断时提示设备异常 |
| 9 | 使用分析工具 | 有实时数据 | 置零、翻转、框选、量尺、可视化调节 | 视图和统计变化 | 工具不可用时给出提示 |
| 10 | 点击采集 | 设备在线且校验通过 | 创建采集任务，持续写入本地数据库 | 按钮变为停止，数据持续保存 | 设备不合法或类型不匹配时拒绝 |
| 11 | 点击停止 | 正在采集 | 停止写入，生成历史记录 | 历史列表出现记录 | 保存失败时保留缓存并提示 |
| 12 | 回放历史 | 存在历史记录 | 加载历史帧，按进度播放 | 2D/3D 视图、统计、图表同步变化 | 历史损坏时提示并跳过损坏帧 |
| 13 | 导出 CSV | 选择历史记录 | 选择路径，生成 CSV | 导出成功，可打开文件 | 导出失败提示原因 |
| 14 | 导入 CSV | 选择 CSV 文件 | 校验文件结构和矩阵长度 | 导入成功进入本地数据列表 | 不符合结构提示“数据有误” |

## 6. 全局状态机

| 状态 | 进入条件 | 可执行操作 | 禁止操作 | 退出条件 |
|---|---|---|---|---|
| 未配置 | 本地无设备配置 | 进入 MAC 配置、录入设备 | 采集、回放实时数据 | 配置保存成功 |
| 未连接 | 有配置但未连接设备 | 一键连接、设备管理、历史查看 | 实时采集 | 连接成功 |
| 连接中 | 用户点击一键连接 | 等待、取消连接（如支持） | 重复点击连接、采集 | 成功或失败 |
| 已连接 | 设备在线且校验通过 | 查看视图、使用工具、开始采集 | 无 | 断开设备或开始采集 |
| 采集中 | 用户点击采集且校验通过 | 停止采集、查看视图、框选统计 | 切换系统类型、删除当前采集 | 停止采集或异常中断 |
| 回放中 | 用户选择历史记录 | 播放、暂停、拖动、倍速、框选回放 | 实时采集 | 退出回放 |
| 导出中 | 用户点击导出 | 等待导出 | 重复导出、删除当前记录 | 导出成功或失败 |
| 异常恢复 | 软件启动发现未完成采集 | 恢复、丢弃、查看缓存 | 直接覆盖缓存 | 用户选择处理方式 |

## 7. 模块一：打开软件后的连接流程

### 7.1 模块目标

| 项目 | 内容 |
|---|---|
| 模块目标 | 完成设备配置、串口连接、设备识别、授权校验和设备参数读取 |
| 业务价值 | 降低现场连接门槛，确保只有合法设备可以采集 |
| 成功结果 | 设备在线，进入实时压力展示 |

### 7.2 当前已实现

| 功能 | 当前状态 | 备注 |
|---|---|---|
| 首次启动检查设备配置 | 已实现 | 无配置进入 MAC 配置页 |
| MAC 地址和类型批量录入 | 已实现 | V2.0 暂沿用 `MAC地址:类型` |
| 一键连接 | 已实现 | 自动扫描串口 |
| 重新连接 | 已实现 | 用于设备异常后恢复 |
| 断开连接 | 已实现 | 用于主动断开设备 |
| 串口扫描 | 已实现 | 识别可用串口 |
| CH340 识别 | 已实现 | 当前硬件连接重点 |
| 波特率自动探测 | 已实现 | 921600 / 1000000 / 3000000 |
| MAC 地址读取 | 已实现 | 设备身份识别 |
| 设备在线状态展示 | 已实现 | 主界面显示 |
| 系统类型切换 | 已实现 | 采集中限制需补齐 |
| 设备管理页 | 已实现 | 添加、列表、系统配置、更新日志、MAC 配置 |

### 7.3 连接业务逻辑

| 业务场景 | 前置条件 | 触发动作 | 系统处理 | 成功结果 | 异常处理 |
|---|---|---|---|---|---|
| 首次启动 | 本地无设备配置 | 打开软件 | 检查设备缓存 | 跳转 MAC 配置页 | 无 |
| 已配置启动 | 本地已有设备配置 | 打开软件 | 加载设备缓存 | 进入主界面 | 缓存损坏时提示重新配置 |
| 一键连接 | 设备已插入 | 点击一键连接 | 扫描串口、探测波特率、读取 MAC | 设备在线 | 未找到设备时提示 |
| 设备类型识别 | 读取到 MAC | 连接流程自动触发 | 优先查本地 MAC 映射 | 得到设备类型 | 未命中时提示配置或禁止采集 |
| 授权校验 | 设备类型已识别 | 连接流程自动触发 | 校验设备合法性 | 允许采集 | 失败时禁止采集 |
| 断开连接 | 设备已连接 | 点击断开 | 关闭串口和实时推送 | 状态变未连接 | 采集中断开需提示风险 |
| 重新连接 | 连接失败或断开 | 点击重新连接 | 清理旧连接并重新扫描 | 连接恢复 | 重试失败提示原因 |

### 7.3.1 连接异常处理重点

一键连接和重新连接是入口级能力。该模块如果卡死，后续展示、数据处理、采集、回放、导出都不可用。因此连接模块必须按“不会卡死、可恢复、可解释”的标准设计。

| 风险 | 影响 | 处理原则 | 优先级 |
|---|---|---|---|
| 连接中卡死 | 用户无法进入实时展示 | 必须有全流程超时和连接锁释放 | P0 |
| 重复点击连接 | 串口状态混乱 | 前端按钮置灰，后端连接锁拦截 | P0 |
| 串口被占用 | 无法打开设备 | 明确提示串口占用，并释放临时资源 | P0 |
| 波特率探测失败 | 设备无法识别 | 尝试候选波特率后返回明确失败原因 | P0 |
| MAC 读取失败 | 无法授权和识别类型 | 禁止采集，提示重新插拔或重连 | P0 |
| 旧连接未释放 | 重连失败或僵尸串口 | 重连前必须关闭旧端口、监听器、parser、定时器 | P0 |
| 采集中断连 | 数据可能丢失 | 保留已采集数据，生成异常中断状态 | P0 |

### 7.3.2 连接状态机

| 状态 | 进入条件 | 可执行操作 | 禁止操作 | 退出条件 |
|---|---|---|---|---|
| 未连接 | 软件启动、有配置但未连接 | 一键连接、历史查看、设备管理 | 采集 | 点击一键连接 |
| 连接中 | 用户点击一键连接 | 等待、取消连接（如支持） | 重复连接、采集、切换设备类型 | 成功、失败或超时 |
| 已连接 | 串口连接、MAC 读取、类型校验成功 | 查看实时数据、使用工具、开始采集、断开 | 重复一键连接 | 断开、异常、采集 |
| 重连中 | 用户点击重新连接或设备异常恢复 | 等待 | 重复重连、采集 | 成功、失败或超时 |
| 连接失败 | 连接流程失败 | 重新连接、查看错误原因 | 采集 | 重连成功或断开 |
| 设备异常 | 连接后连续坏帧、空帧或设备断开 | 重新连接、断开 | 采集 | 恢复或断开 |
| 采集中断连 | 采集中设备断开 | 停止采集、保存已采集数据、重新连接 | 删除当前未处理数据 | 用户处理完成 |

### 7.3.3 一键连接主流程

| 步骤 | 系统动作 | 成功结果 | 异常处理 |
|---|---|---|---|
| 1 | 前端点击一键连接，按钮进入连接中 | 防止重复点击 | 已连接或连接中时直接提示 |
| 2 | 后端创建连接锁 | 同一时间只有一个连接任务 | 锁已存在时返回连接中 |
| 3 | 清理残留串口资源 | 避免旧连接影响新连接 | 清理失败记录日志但继续尝试 |
| 4 | 扫描串口 | 得到候选串口列表 | 无串口提示未检测到设备 |
| 5 | 筛选 CH340 设备 | 得到目标设备端口 | 无 CH340 提示检查连接线 |
| 6 | 探测波特率 | 找到可用波特率 | 候选波特率均失败则返回失败 |
| 7 | 建立稳定串口连接 | 打开正式连接 | 打开失败释放资源 |
| 8 | 读取 MAC | 获取设备身份 | MAC 读取失败则禁止采集 |
| 9 | 匹配设备类型 | 得到系统类型 | 未配置或不匹配则提示 |
| 10 | 绑定数据监听 | 开始接收实时帧 | 绑定失败释放连接 |
| 11 | 推送连接成功状态 | 前端进入实时展示 | 任一步失败都释放连接锁 |

### 7.3.4 连接锁和超时规则

| 项目 | 规则 | 优先级 |
|---|---|---|
| 前端按钮锁 | 连接中/重连中按钮置灰，防止重复点击 | P0 |
| 后端连接锁 | `/connPort`、`/rescanPort` 同一时间只允许一个任务 | P0 |
| 锁释放 | 成功、失败、超时、异常都必须释放锁 | P0 |
| 全流程超时 | 一键连接建议 20 秒内返回结果 | P0 |
| 串口扫描超时 | 建议 3 秒 | P0 |
| 单个波特率探测超时 | 建议 2 秒 | P0 |
| 稳定连接打开超时 | 建议 2 秒，最多重试 3 次 | P0 |
| MAC 读取超时 | 建议 30 秒 | P0 |
| 前端请求超时 | 建议 15-20 秒，超时后提示用户 | P0 |

### 7.3.5 重新连接规则

| 场景 | 是否允许重连 | 处理规则 | 优先级 |
|---|---|---|---|
| 未连接 | 允许 | 等同一键连接 | P0 |
| 连接失败 | 允许 | 清理旧状态后重新扫描 | P0 |
| 已连接 | 允许 | 先断开旧连接，再重新连接 | P0 |
| 设备异常 | 允许 | 清理旧 parser、listener、timer 后重连 | P0 |
| 连接中 | 不允许 | 提示连接中，请稍候 | P0 |
| 重连中 | 不允许 | 提示重连中，请稍候 | P0 |
| 采集中 | 谨慎允许 | 建议先提示会中断采集，保留已采集数据 | P0 |
| 回放中 | 允许但不影响回放 | 回放使用历史数据，不依赖实时连接 | P1 |

### 7.3.6 后端资源释放规则

| 资源 | 释放时机 | 规则 |
|---|---|---|
| 临时探测串口 | 波特率探测结束后 | 成功或失败都必须 close |
| 稳定连接串口 | 断开、重连、连接失败后 | 必须 close 并清空引用 |
| parser | 断开、重连、异常后 | 必须移除监听 |
| data listener | 断开、重连、异常后 | 必须解绑，防止重复推送 |
| 定时器 | 断开、重连、异常后 | 必须 clear |
| 数据缓存 | 重连前 | 按设备和系统类型清理 |
| 连接锁 | 成功、失败、超时、异常后 | 必须释放 |

### 7.3.7 连接错误码建议

| 错误码 | 含义 | 用户提示 |
|---|---|---|
| CONN_BUSY | 当前已有连接任务 | 正在连接，请稍候 |
| NO_PORT | 未检测到串口 | 未检测到设备，请检查连接 |
| NO_CH340 | 未识别到 CH340 | 未识别到传感器设备 |
| PORT_BUSY | 串口被占用 | 串口被占用，请关闭其他软件后重试 |
| BAUD_FAIL | 波特率探测失败 | 设备通信失败，请重新插拔后重试 |
| OPEN_FAIL | 串口打开失败 | 设备连接失败，请重试 |
| MAC_FAIL | MAC 读取失败 | 设备识别失败，请重新连接 |
| TYPE_UNKNOWN | 设备类型未知 | 未找到设备类型配置 |
| AUTH_FAIL | 授权失败 | 当前设备未授权，不能采集 |
| DEVICE_DISCONNECTED | 设备断开 | 设备已断开，请重新连接 |
| CONNECT_TIMEOUT | 连接超时 | 连接超时，请重新连接 |

### 7.3.8 `/startCol` 采集前强校验

`/startCol` 是进入数据采集的最后一道门。即使前端按钮状态异常，后端也必须再次校验设备是否合法，不能只依赖前端状态。

| 校验项 | 通过条件 | 失败处理 | 错误码 | 用户提示 | 优先级 |
|---|---|---|---|---|---|
| 设备连接状态 | 当前存在稳定串口连接，且设备在线 | 拒绝开始采集 | DEVICE_DISCONNECTED | 设备未连接，请先连接设备 | P0 |
| MAC 已读取 | 当前连接已成功读取 MAC | 拒绝开始采集 | MAC_FAIL | 设备识别失败，请重新连接 | P0 |
| MAC 已配置 | MAC 命中本地设备配置 | 拒绝开始采集 | TYPE_UNKNOWN | 当前设备未配置，请先录入设备信息 | P0 |
| 设备已授权 | 当前 MAC 或设备密钥通过授权校验 | 拒绝开始采集 | AUTH_FAIL | 当前设备未授权，不能采集 | P0 |
| 类型匹配 | 当前连接设备类型与当前系统类型一致 | 拒绝开始采集 | TYPE_MISMATCH | 当前设备类型与系统类型不匹配 | P0 |
| 矩阵尺寸有效 | 设备类型能映射到明确矩阵尺寸 | 拒绝开始采集 | MATRIX_SIZE_INVALID | 设备点阵配置异常，不能采集 | P0 |
| 采集未进行中 | 当前没有未结束采集任务 | 拒绝重复采集 | COLLECT_BUSY | 正在采集，请勿重复开始 | P0 |
| 方向状态稳定 | 当前不处于方向修改中 | 拒绝开始采集 | DIRECTION_UPDATING | 方向配置正在更新，请稍后采集 | P1 |

### 7.3.9 连接错误码落地规则

| 规则 | 说明 | 优先级 |
|---|---|---|
| 前后端共用同一套错误码 | 后端接口返回 `code/message/detail`，前端只负责展示和状态切换 | P0 |
| 用户提示必须可执行 | 提示文案要告诉用户下一步怎么处理，例如重新插拔、关闭占用软件、录入 MAC | P0 |
| 日志必须保留技术细节 | 日志记录端口号、波特率、MAC、设备类型、失败阶段、原始错误对象 | P0 |
| 同一错误只提示一次 | 连续坏帧或连接失败不应弹窗刷屏，同类错误需节流 | P0 |
| 错误后必须释放资源 | 失败、超时、取消、异常退出都必须释放串口、监听器、parser、连接锁 | P0 |

### 7.4 设备配置业务逻辑

| 规则 | 说明 | 优先级 |
|---|---|---|
| 配置格式必须可解析 | V2.0 暂沿用 `MAC地址:类型`，后续如解析歧义明显再升级为 `MAC=类型` | P1 |
| 每次录入只能插一个设备 | 避免多个串口同时插入导致 MAC 识别混乱 | P0 |
| MAC 必须唯一 | 同一个 MAC 不允许重复配置不同类型 | P0 |
| 类型必须在支持列表内 | 如 carY-sit、carY-back、endi-sit、endi-back 等 | P0 |
| 配置保存后写入本地缓存 | 支持离线使用 | P0 |
| 清空缓存不应删除历史数据 | 设备缓存和采集历史是两类数据 | P0 |

### 7.5 设备参数业务逻辑

| 字段 | 来源 | 是否必须 | 是否展示 | 是否入库 | 是否导出 | 备注 |
|---|---|---|---|---|---|---|
| MAC 地址 | 设备读取 | 是 | 是 | 是 | 是 | 设备唯一身份 |
| 设备类型 | MAC 映射或设备返回 | 是 | 是 | 是 | 是 | 当前以本地映射为主 |
| 串口号 | 串口扫描 | 是 | 是 | 建议 | 建议 | 用于排查 |
| 波特率 | 自动探测 | 是 | 是 | 建议 | 建议 | 921600/1000000/3000000 |
| 采样率 | 设备或后端计算 | 是 | 是 | 是 | 是 | 用于回放和 CSV |
| 固件版本 | 硬件/固件返回 | 是 | 建议 | 建议 | 建议 | 已确认：硬件会发送版本，软件读取并记录 |
| 点阵数量 | 类型配置或设备返回 | 是 | 是 | 是 | 是 | 影响矩阵解析 |
| 压力范围 | 暂不声明标准物理压力范围 | 否 | 否 | 否 | 否 | V2.0 暂使用软件内部压力单位，不对外声明物理单位 |

### 7.6 待补齐项

| 编号 | 问题 | 优先级 | 处理建议 |
|---|---|---|---|
| 连接-01 | 设备类型识别口径不统一 | P0 | 明确 V2.0 当前以 MAC 映射为主，固件返回为后续升级 |
| 连接-02 | 授权失败是否拦截采集不清楚 | P0 | `/startCol` 必须拦截非法设备、类型不匹配、MAC 未配置 |
| 连接-03 | MAC 配置格式存在歧义 | P1 | V2.0 暂沿用 `MAC地址:类型`，后续根据实际录入问题再改为 `MAC=类型` |
| 连接-04 | 设备参数字段不完整 | P0 | 固件版本由硬件返回；点阵数量按设备类型配置；压力范围 V2.0 暂不声明物理单位 |

### 7.7 验收标准

| 编号 | 验收项 | 通过标准 | 优先级 |
|---|---|---|---|
| 验收-连接-01 | 首次启动 | 无配置时进入 MAC 配置页 | P0 |
| 验收-连接-02 | 一键连接 | 合法设备可连接成功 | P0 |
| 验收-连接-03 | 非法设备 | 非法设备不能开始采集 | P0 |
| 验收-连接-04 | 参数展示 | MAC、类型、串口、波特率、采样率可见 | P0 |
| 验收-连接-05 | 断开处理 | 设备断开后状态变化并提示 | P0 |


### 7.8 PRD1.1 增量补充

#### 7.8.1 MAC / Unique ID 强校验

| 场景 | 规则 | 用户收益 |
|---|---|---|
| 新增设备配置 | 保存前校验 MAC、Unique ID、设备类型和设备大类 | 减少错误设备被写入配置 |
| 重复配置 | 同一 MAC 或 Unique ID 不允许重复保存 | 避免一台设备被识别成多个类型 |
| 不支持设备类型 | 阻止保存，并提示不支持的类型 | 避免后续连接后进入未知设备状态 |
| 批量保存 | 先整体验证，再清空旧缓存并写入新配置 | 避免部分成功、部分失败 |

#### 7.8.2 一键连接与重连补充

| 功能 | PRD1.1 规则 |
|---|---|
| 一键连接 | 扫描串口、识别 CH340、探测波特率、读取 MAC、匹配设备类型后进入实时数据流 |
| 重新连接 | 重连前完整释放旧串口、parser、listener、timer、缓存和发送定时器 |
| 连接失败 | 必须恢复按钮可操作状态，并显示失败原因 |
| 退出回放/对比 | 如果设备仍在线，状态恢复为已连接，不显示未连接 |
| 打包缓存 | serial cache 写入可写目录，禁止写入 `resources/app.asar` |

#### 7.8.3 PRD1.1 验收标准

| 编号 | 验收项 | 通过标准 |
|---|---|---|
| CONN-1.1-01 | MAC 强校验 | 重复 MAC、重复 Unique ID、不支持设备类型均不能保存 |
| CONN-1.1-02 | 批量保存 | 任一配置错误时整批保存失败，并提示具体错误 |
| CONN-1.1-03 | 重连清理 | 重连前旧串口和监听器被释放，不出现旧端口占用 |
| CONN-1.1-04 | 打包缓存 | 打包后不出现 `app.asar/serial_cache.json ENOENT` |
| CONN-1.1-05 | 状态恢复 | 退出回放或对比后，设备在线时 header 显示已连接 |

## 8. 模块二：设备数据接收与处理

### 8.1 模块目标

| 项目 | 内容 |
|---|---|
| 模块目标 | 把设备串口原始数据转换成后续展示、统计、采集和导出可使用的标准压力数据 |
| 模块位置 | 位于“设备连接流程”和“展示视图”之间 |
| 业务价值 | 保证后续所有压力图、统计指标、采集数据和 CSV 数据可信 |
| 成功结果 | 生成结构稳定、尺寸正确、可追溯的原始压力矩阵和推送数据 |

### 8.2 为什么需要单独拆这个模块

| 问题 | 说明 |
|---|---|
| 连接成功不等于数据可用 | 串口连接只是建立通道，还需要确认设备帧能被正确解析 |
| 展示视图依赖处理后数据 | 2D/3D 视图不能直接消费串口原始字节，需要标准矩阵 |
| 统计指标依赖数据口径 | 平均压强、最大压强、面积等都依赖矩阵尺寸、单位和有效点规则 |
| 采集和导出依赖原始数据 | 如果处理层没有定义 rawData，后续很容易把显示数据误当原始数据保存 |
| 异常定位需要分层 | 设备断连、坏帧、矩阵错位、显示异常需要区分是连接问题、解析问题还是渲染问题 |

### 8.3 当前已实现

| 功能 | 当前状态 | 备注 |
|---|---|---|
| 串口数据接收 | 已实现 | 由串口监听和数据帧处理负责 |
| 数据帧分隔 | 已实现 | 根据协议分隔符识别帧 |
| 波特率探测时帧校验 | 已实现 | 用于判断候选波特率是否可用 |
| 设备类型对应解析函数 | 已实现 | hand、bed、endi、carY 等存在不同转换函数 |
| 一维点阵转矩阵 | 已实现 | 按系统类型转换为对应尺寸 |
| 多系统矩阵尺寸配置 | 已实现 | 如 32x32、46x46、50x64、64x64 |
| WebSocket 实时推送 | 已实现 | 后端推送前端消费 |
| 采样率/HZ 处理 | 已实现或部分实现 | V2.0 以后端按帧时间计算为准，硬件返回值用于记录和校验 |
| 历史回放数据结构 | 已实现 | 与实时数据结构需保持一致 |

### 8.4 输入输出

| 类型 | 内容 | 来源/去向 | 说明 |
|---|---|---|---|
| 输入 | 串口原始字节流 | 设备串口 | 设备持续发送的原始数据 |
| 输入 | 设备类型 | 连接模块 | 决定使用哪套解析规则 |
| 输入 | 系统类型 | 前端/后端状态 | 决定矩阵尺寸和展示结构 |
| 输入 | 当前波特率 | 连接模块 | 影响数据帧接收速度 |
| 输出 | 原始帧 rawFrame | 数据处理模块内部 | 用于调试和追溯 |
| 输出 | 原始矩阵 rawData | 展示、统计、采集模块 | 设备原始方向和原始压力数据 |
| 输出 | 推送数据 payload | WebSocket | 前端实时展示使用 |
| 输出 | 采样率 HZ | 展示、历史、CSV | 后端按帧时间计算，用于显示、回放和导出 |
| 输出 | 硬件返回采样率 | 历史、CSV、日志 | 仅作为记录和校验，不作为回放主时钟 |
| 输出 | 异常状态 | 前端提示、日志 | 如坏帧、断帧、矩阵长度不匹配 |

### 8.5 主处理流程

| 步骤 | 处理动作 | 输入 | 输出 | 异常处理 |
|---|---|---|---|---|
| 1 | 接收串口原始字节 | 设备字节流 | 缓冲区数据 | 无数据时保持等待 |
| 2 | 查找帧分隔符 | 缓冲区数据 | 候选数据帧 | 找不到分隔符则继续累积或丢弃异常片段 |
| 3 | 校验帧完整性 | 候选数据帧 | 完整数据帧 | 长度不匹配则丢弃并记录 |
| 4 | 识别设备类型 | MAC 映射/设备类型 | 解析规则 | 类型未知则不进入展示和采集 |
| 5 | 解析压力点数组 | 完整数据帧 | 一维压力数组 | 解析失败则丢弃该帧 |
| 6 | 转换矩阵 | 一维压力数组 + 系统类型 | rawData 矩阵 | 长度和矩阵尺寸不匹配则报错 |
| 7 | 计算采样率 | 后端接收帧时间戳 | HZ | 以后端帧间隔计算为准，异常波动时标记；硬件返回采样率仅记录和校验 |
| 8 | 生成推送数据 | rawData + metadata | WebSocket payload | 推送失败时记录连接状态 |
| 9 | 分发下游模块 | payload | 展示、统计、采集 | 下游失败不应破坏原始数据 |

### 8.6 设备类型与矩阵转换规则

| 设备/系统类型 | 原始数据处理 | 矩阵尺寸 | 输出对象 | 待确认 |
|---|---|---|---|---|
| `endi-back` | endi 靠背转换规则 | 50 x 64 | 靠背矩阵 | 否 |
| `endi-sit` | endi 坐垫转换规则 | 46 x 46 | 坐垫矩阵 | 否 |
| `carY-back` | carY 靠背转换规则 | 32 x 32 | 靠背矩阵 | carY 分压规则 |
| `carY-sit` | carY 坐垫转换规则 | 32 x 32 | 坐垫矩阵 | carY 分压规则 |
| `car-back` | 汽车靠背转换规则 | 32 x 32 | 靠背矩阵 | 否 |
| `car-sit` | 汽车坐垫转换规则 | 32 x 32 | 坐垫矩阵 | 否 |
| `bed` | 床垫转换规则 | 32 x 32 | 床垫矩阵 | 否 |
| `hand` | 手部/坐垫转换规则 | 32 x 32 | 单矩阵 | 否 |
| `foot` | 脚部转换规则 | 32 x 32 或配置尺寸 | 脚部矩阵 | 本版不做最终化 |
| `bigHand` | 4096 点阵转换规则 | 64 x 64 | 高密度矩阵 | 本版不做最终化 |

### 8.6.1 采样率 HZ 业务规则

| 项目 | V2.0 规则 | 说明 |
|---|---|---|
| 主采样率来源 | 后端按连续帧接收时间计算 | 用于界面展示、回放速度、CSV 时间轴和数据对比 |
| 硬件返回采样率 | 记录到 metadata、历史数据和 CSV | 用于追溯和校验，不直接覆盖主采样率 |
| 采样率异常 | 连续 2 秒为 0 或明显跳变时标记异常 | 不立即断开连接，但需要提示数据刷新异常 |
| 回放使用 | 优先使用历史记录中的后端计算采样率和帧时间戳 | 缺失时按帧序号和默认帧间隔降级播放 |
| 数据对比使用 | 采样率不同仍允许对比 | 需要提示“采样率不同，按进度对齐” |

### 8.6.2 foot 与 bigHand 范围说明

| 项目 | 本版规则 | 后续处理 |
|---|---|---|
| `foot` 矩阵尺寸 | V2.0 不做最终尺寸确认 | 后续根据真实脚部硬件协议或设备配置单独补充 |
| `bigHand` 矩阵尺寸 | V2.0 不做新增业务优化 | 当前已有 64 x 64 能力可继续保留，但不作为本轮重点 |
| 对研发影响 | 不阻塞汽车座椅、床垫、坐垫/手部主流程 | 如涉及脚部或 4096 点阵交付，需要另开专项确认 |

### 8.7 数据层业务逻辑

| 数据层 | 生成位置 | 业务含义 | 是否可被修改 | 下游用途 |
|---|---|---|---|---|
| rawFrame | 后端串口接收后 | 设备原始帧或原始字节片段 | 不应修改 | 调试、异常定位 |
| rawPointArray | 后端解析后 | 一维压力点数组 | 不应修改 | 矩阵转换 |
| rawData | 后端或前端矩阵转换后 | 业务原始压力矩阵，经过方向修正后作为全链路基准矩阵 | 可被翻转/旋转修改；不受颜色、润滑、噪点消除、框选污染 | 展示、统计、采集、导出 |
| backendProcessedData | 后端处理数据流 | 在 rawData 基础上做后端级处理后的矩阵，例如噪点消除 | 可由后端数据处理规则生成 | 展示、统计、采集、导出 |
| displayData | 前端处理后 | 用于界面展示的矩阵 | 可受颜色、润滑、高度、响应速度影响 | 2D/3D 展示 |
| calculatedData | 统计模块处理后 | 用于指标计算的矩阵 | 受置零、框选、后端处理数据流影响 | 平均值、最大值、面积等 |
| storedData | 采集模块处理后 | 入库保存的数据 | 口径必须明确 | 历史回放 |
| exportedData | 导出模块处理后 | CSV 输出数据 | 口径必须明确 | 科研分析、外部导入 |

说明：V2.0 将 `rawFrame/rawPointArray` 作为设备原始输入保留；`rawData` 是软件业务原始矩阵。用户执行翻转/旋转后，`rawData` 需要按修正后的方向重建，后续可视化、统计、采集、回放、导出都基于修正后的 `rawData`。

### 8.7.1 标准数据字典

| 数据对象 | 字段 | 类型 | 单位/格式 | 来源 | 是否必须 | 说明 |
|---|---|---|---|---|---|---|
| rawFrame | `frame_id` | string/number | 自增或设备帧号 | 后端生成/设备返回 | 是 | 用于定位单帧问题 |
| rawFrame | `received_at` | number | 毫秒时间戳 | 后端接收时间 | 是 | 用于计算采样率和日志追溯 |
| rawFrame | `hardware_sample_rate_hz` | number | HZ | 硬件返回 | 建议 | 用于记录和校验，不作为主采样率 |
| rawFrame | `port` | string | 串口名 | 连接模块 | 建议 | 现场排查使用 |
| rawFrame | `baud_rate` | number | bps | 连接模块 | 建议 | 921600/1000000/3000000 |
| rawFrame | `raw_bytes` | string/buffer | 原始字节 | 串口 | 调试保留 | 默认不进入 CSV |
| rawPointArray | `points` | number[] | 软件压力原始值 | 解析模块 | 是 | 一维点阵数组 |
| rawPointArray | `point_count` | number | 个 | 解析模块 | 是 | 必须等于矩阵宽 x 高 |
| rawData | `matrix` | number[][] | 软件压力原始值 | 矩阵转换 + 方向修正 | 是 | 业务原始矩阵；受翻转/旋转影响，不受颜色、框选、润滑、噪点消除影响 |
| rawData | `width` | number | 点 | 设备类型配置 | 是 | 矩阵列数 |
| rawData | `height` | number | 点 | 设备类型配置 | 是 | 矩阵行数 |
| rawData | `matrix_key` | string | sit/back/overall/foot 等 | 系统类型 | 是 | 区分坐垫、靠背和其他对象 |
| rawData | `device_type` | string | 类型枚举 | MAC 映射/设备返回 | 是 | 与连接模块一致 |
| rawData | `direction` | object/string | 方向枚举 | 工具模块 | 是 | 记录左右翻转、上下翻转、累计旋转角度 |
| backendProcessedData | `matrix` | number[][] | 后端处理后的软件压力值 | 后端数据处理 | 条件必须 | 噪点消除开启时生成，不覆盖 rawData |
| backendProcessedData | `process_flags` | object | 处理标记 | 后端数据处理 | 条件必须 | 例如 `noise_removed=true` |
| displayData | `matrix` | number[][] | 显示值 | 前端处理 | 是 | 可受方向、置零、颜色、润滑影响 |
| displayData | `direction` | object/string | 方向枚举 | 工具模块 | 是 | 包含翻转和旋转状态 |
| calculatedData | `scope` | string | all/selection | 统计模块 | 是 | 全量或框选区域 |
| calculatedData | `avg_pressure` | number | 当前软件压力单位 | 统计模块 | 是 | 默认排除 0 和低于阈值点 |
| calculatedData | `max_pressure` | number | 当前软件压力单位 | 统计模块 | 是 | 当前范围最大值 |
| calculatedData | `min_pressure_non_zero` | number | 当前软件压力单位 | 统计模块 | 是 | 非零最小值 |
| calculatedData | `pressure_sum` | number | 当前软件压力单位求和 | 统计模块 | 是 | 有效点求和 |
| calculatedData | `contact_area` | number | cm² | 统计模块 | 是 | 有效点数 x 单点面积 |
| calculatedData | `active_sensor_count` | number | 个 | 统计模块 | 是 | 大于有效阈值的点数 |
| calculatedData | `center_of_pressure` | object | x/y 坐标 | 统计模块 | 是 | 基于修正方向矩阵 |
| storedData | `record_id` | string | UUID/数据库 ID | 采集模块 | 是 | 历史记录唯一标识 |
| storedData | `frames` | array | 帧数组 | 采集模块 | 是 | 保存全量矩阵，不只保存框选 |
| storedData | `metadata` | object | 结构化对象 | 采集模块 | 是 | 设备、方向、置零、采样率等 |
| exportedData | `csv_format_version` | string | 如 1.0 | 导出模块 | 是 | 用于导入兼容 |
| exportedData | `real_data` | string/json | 矩阵序列化 | 导出模块 | 是 | 全量矩阵字段 |
| exportedData | `select_data` | string/json | 派生区域数据 | 导出模块 | 否 | 框选统计或区域矩阵 |
| exportedData | `max_pressure_coord` | string/object | row/column | 导出模块 | 是 | 最大值坐标 |
| exportedData | `selection_row_range` | string | startRow-endRow | 导出模块 | 条件必须 | 有框选时导出 |
| exportedData | `selection_column_range` | string | startColumn-endColumn | 导出模块 | 条件必须 | 有框选时导出 |

### 8.8 异常处理业务逻辑

| 异常类型 | 判断条件 | 系统处理 | 用户提示 | 是否阻塞采集 |
|---|---|---|---|---|
| 空帧 | 收到空数据或无有效压力点 | 丢弃该帧 | 不一定提示，可记录日志 | 否 |
| 坏帧 | 帧头/帧尾/分隔符异常 | 丢弃该帧并记录 | 高频出现时提示设备数据异常 | 视出现频率而定 |
| 帧长度不匹配 | 解析长度不符合设备类型 | 丢弃该帧 | 提示数据长度异常 | 是 |
| 设备类型未知 | MAC 未命中且设备未返回类型 | 不解析为有效矩阵 | 提示设备类型未知 | 是 |
| 矩阵尺寸不匹配 | 点数无法映射到配置矩阵 | 拒绝进入展示/采集 | 提示设备配置异常 | 是 |
| 采样率异常 | 后端计算 HZ 长时间为 0 或明显跳变 | 记录并提示，同时保留硬件返回采样率用于排查 | 视情况而定 |
| WebSocket 推送失败 | 前端连接断开 | 保留后端最新数据，等待重连 | 提示实时连接异常 | 不一定 |
| 连续坏帧 | 一段时间内坏帧超过阈值 | 标记设备数据异常 | 提示检查设备或连接 | 是 |

### 8.8.1 异常帧阈值和提示规则

| 场景 | 建议阈值 | 系统处理 | 用户提示 | 日志要求 | 优先级 |
|---|---|---|---|---|---|
| 单个坏帧 | 1 帧 | 丢弃，不中断实时展示 | 不提示 | 记录 bad_frame_count | P0 |
| 短时间坏帧增多 | 1 秒内坏帧率超过 10% | 保持连接，标记数据质量下降 | 设备数据不稳定，请检查连接 | 记录窗口内总帧、坏帧、比例 | P0 |
| 连续坏帧 | 连续 10 帧 | 暂停采集写入异常帧，状态进入设备异常 | 设备数据异常，请重新连接 | 记录起止时间和最后一帧错误 | P0 |
| 帧长度不匹配 | 任意 1 帧 | 丢弃该帧；连续出现按设备异常处理 | 数据长度异常，请检查设备类型 | 记录期望长度、实际长度、设备类型 | P0 |
| 矩阵尺寸不匹配 | 任意 1 次转换失败 | 禁止展示为有效矩阵，禁止采集 | 点阵配置异常，不能采集 | 记录 matrix_key、width、height、point_count | P0 |
| 采样率为 0 | 连续 2 秒 | 保持连接，标记 HZ 异常 | 数据刷新异常，请检查设备 | 记录最近帧时间戳 | P1 |
| WebSocket 推送失败 | 连续 3 次 | 保留后端数据，前端重连 | 实时显示连接异常 | 记录连接 ID 和失败原因 | P1 |

说明：阈值是 V2.0 默认建议值，研发可根据真实设备帧率微调，但必须保证“单帧异常不打断，连续异常可提示和恢复”。

### 8.9 与下游模块的关系

| 下游模块 | 依赖数据 | 依赖规则 |
|---|---|---|
| 展示视图 | rawData / backendProcessedData / displayData | 数据尺寸、设备类型、坐垫/靠背 key 必须正确；翻转/旋转后的 rawData 是展示基准 |
| 工具模块 | rawData / backendProcessedData / displayData / calculatedData | 翻转/旋转会修改 rawData；置零和噪点消除生成派生数据；框选不丢全量 |
| 数据统计 | calculatedData | 统计口径依赖置零、框选、后端处理数据流 |
| 采集回放 | storedData + metadata | 入库数据必须可追溯设备、方向、采样率 |
| CSV 导出 | exportedData + metadata | 导出字段必须能解释数据口径 |
| 数据对比 | rawData 或标准化数据 | 对比前需要统一尺寸、方向、采样率 |

### 8.10 待补齐项

| 编号 | 问题 | 优先级 | 处理建议 |
|---|---|---|---|
| 数据处理-01 | rawFrame、rawData、displayData 等命名未统一 | P0 | 建立数据字典，统一全链路字段名 |
| 数据处理-02 | 坏帧、断帧、长度不匹配处理规则不完整 | P0 | 补异常阈值和提示规则 |
| 数据处理-03 | 采样率 HZ 来源和计算方式不清 | P0 | 已明确：以后端按帧时间计算为准，硬件返回值作为记录和校验 |
| 数据处理-04 | 矩阵尺寸与设备类型映射需补全 | P0 | 主流程设备已补；foot 和 bigHand 本版不做最终化 |
| 数据处理-05 | carY 分压属于处理层还是统计层不清 | P0 | 以当前业务值口径输出，后续如要区分物理单位需单独标定 |
| 数据处理-06 | 推送 payload 字段缺少字典 | P1 | 补 WebSocket payload 字段说明 |

### 8.11 验收标准

| 编号 | 验收项 | 通过标准 | 优先级 |
|---|---|---|---|
| 验收-数据处理-01 | 原始帧解析 | 合法设备数据帧可被稳定解析 | P0 |
| 验收-数据处理-02 | 矩阵尺寸 | 不同设备类型转换后的矩阵尺寸正确 | P0 |
| 验收-数据处理-03 | 类型未知处理 | 设备类型未知时不允许展示为有效数据，也不允许采集 | P0 |
| 验收-数据处理-04 | 坏帧处理 | 单个坏帧被丢弃，不影响后续正常帧 | P0 |
| 验收-数据处理-05 | 连续异常处理 | 连续坏帧或长度不匹配时提示设备数据异常 | P0 |
| 验收-数据处理-06 | 采样率 | 后端计算 HZ 可显示、可记录、可用于回放和 CSV，硬件采样率可记录和校验 | P0 |
| 验收-数据处理-07 | 数据层隔离 | 颜色、润滑、框选不污染 rawData；翻转/旋转会重建业务 rawData 并持久化方向 | P0 |
| 验收-数据处理-08 | 推送稳定 | WebSocket 推送数据可被前端稳定消费 | P0 |


### 8.12 PRD1.1 增量补充

#### 8.12.1 数据质量信息

| 字段 | 说明 | 使用场景 |
|---|---|---|
| `sampleRateHz` | 当前采样率 | 顶部状态、对比采样率提示、导出 metadata |
| `frameIntervalMs` | 当前帧间隔 | 判断数据是否稳定 |
| `matrixMeta` | 当前矩阵类型、尺寸、key | 前端选择显示对象、导出和对比 |
| `dataQuality` | 当前帧质量状态 | 判断是否可展示、采集、回放 |

#### 8.12.2 异常帧处理补充

| 场景 | PRD1.1 规则 |
|---|---|
| 实时空帧 | 不进入有效数据流，不更新 UI 主数据 |
| 矩阵缺失 | 标记为 `matrix_error`，不得进入采集入库 |
| 矩阵长度不匹配 | 标记为异常帧，前端不崩溃 |
| 非法数值 | 跳过该帧或按异常帧处理 |
| 回放损坏帧 | 自动跳过；连续异常达到阈值时停止并提示 |

#### 8.12.3 endi 靠背 1024 线序和有效区

| 项 | PRD1.1 规则 |
|---|---|
| 原始长度 | endi 靠背原始数据长度为 1024 |
| 原始矩阵 | 按 32 x 32 读取 |
| 物理矩阵 | 根据实际线序生成 25 x 32 靠背物理矩阵 |
| 显示矩阵 | 使用与坐垫一致的线性插值扩展为 50 x 64 |
| 顶部有效区 | 顶部 9 行 11 列为窄区有效点，其余补 0 点不展示 |
| 0 值规则 | 有效区真实 0 值仍展示；补 0 区域隐藏 |
| 一致性 | 2D 数字、3D 点图、原始数据、对比页共用同一有效 mask |

#### 8.12.4 PRD1.1 验收标准

| 编号 | 验收项 | 通过标准 |
|---|---|---|
| DATA-1.1-01 | 数据质量字段 | 实时 payload 中能看到采样率、帧间隔、矩阵元信息和质量状态 |
| DATA-1.1-02 | 异常帧 | 坏帧不进入采集和历史有效数据 |
| DATA-1.1-03 | 靠背线序 | 1024 原始数据能正确映射到 50 x 64 显示矩阵 |
| DATA-1.1-04 | 线性插值 | 靠背点图过渡方式与坐垫一致 |
| DATA-1.1-05 | 补 0 隐藏 | 靠背补 0 区域在 2D、3D、对比页表现一致 |

## 9. 模块三：连接之后的展示视图

### 8.1 模块目标

| 项目 | 内容 |
|---|---|
| 模块目标 | 连接成功后实时展示压力数据 |
| 核心视图 | 2D 数字、3D 点图、3D 数字、3D 模型 |
| 核心场景 | 汽车坐垫和靠背实时压力展示 |

### 8.2 当前已实现

| 功能 | 当前状态 | 备注 |
|---|---|---|
| 3D 模型视图 | 已实现 | 座椅类支持 |
| 2D 数字视图 | 已实现 | 显示点阵数值 |
| 3D 数字视图 | 已实现 | 需验证性能 |
| 整体/靠背/坐垫切换 | 已实现 | 座椅类支持 |
| 靠背/坐垫动画切换 | 已实现 | 3D 场景 |
| 3D 左键旋转中心修正 | 已实现 | 已优化 |
| 缩放 | 已实现 | 范围需确认 |
| 视图重置 | 已实现 | 恢复默认视角 |
| 放大镜 | 已实现 | 2D 数字视图 |

### 8.3 展示视图业务逻辑

| 视图 | 进入条件 | 数据来源 | 展示内容 | 可用工具 | 业务规则 |
|---|---|---|---|---|---|
| 2D 数字视图 | 设备在线并选择 2D 数字 | 实时压力矩阵 | 每个传感点数值 | 框选、量尺、放大镜 | 用于精确查看点值 |
| 3D 点图 | 设备在线并选择 3D 点图 | 实时压力矩阵 | 点阵高度/颜色变化 | 视角、缩放、重置 | 用于立体观察压力分布 |
| 3D 模型 | 座椅类设备 | 压力矩阵 + 模型 | 座椅模型和压力点 | 视角、整体/靠背/坐垫切换 | 用于演示和空间理解 |
| 3D 数字 | 设备在线并选择 3D 数字 | 实时压力矩阵 | 3D 数字标签 | 视角、缩放 | 高密度点阵需注意性能 |

### 8.4 视图切换业务逻辑

| 切换动作 | 前置条件 | 系统处理 | 数据影响 | 异常处理 |
|---|---|---|---|---|
| 切换 2D/3D | 设备在线 | 更换渲染组件 | 不改变原始数据 | 组件加载失败时回退默认视图 |
| 切换整体 | 座椅类设备 | 显示坐垫和靠背整体 | 不改变原始数据 | 非座椅类隐藏入口 |
| 切换坐垫 | 座椅类设备 | 仅显示坐垫数据 | 不改变原始数据 | 无坐垫数据时提示 |
| 切换靠背 | 座椅类设备 | 仅显示靠背数据 | 不改变原始数据 | 无靠背数据时提示 |
| 重置视图 | 任意视图 | 恢复默认缩放、位置、视角 | 不改变原始数据 | 无 |

### 8.5 缩放业务逻辑

| 项目 | 规则 | 待确认 |
|---|---|---|
| 缩放入口 | 鼠标滚轮和缩放按钮 | 否 |
| 缩放对象 | 当前展示视图 | 否 |
| 缩放范围 | V2.0 确认为 10%-300% | 否 |
| 缩放影响 | 只影响显示，不影响采集、统计、导出 | 否 |
| 超出范围 | 到达上限/下限后不再继续缩放 | 否 |

### 8.5.1 3D 点图颜色映射业务逻辑

| 项目 | V2.0 规则 | 说明 |
|---|---|---|
| 设置方式 | 用户直接设置颜色映射上限值 | 不再只使用百分比作为唯一设置方式 |
| 默认下限 | 0 或当前过滤后的最小有效值 | 无压力点按 0 处理 |
| 默认上限 | 使用系统默认压力上限 | 用户修改后按用户设置值生效 |
| 超上限数据 | 按最高颜色显示 | 不截断原始数值，统计和导出仍使用真实值 |
| 低于过滤值数据 | 按无效点或最低颜色显示 | 受当前过滤值影响 |
| 影响范围 | 只影响 3D 点图颜色显示 | 不改变 rawData、统计、采集、回放和 CSV |
| 持久化 | 按设备类型或用户配置保存 | 下次打开同类型设备时可沿用 |

### 8.6 待补齐项

| 编号 | 问题 | 优先级 | 处理建议 |
|---|---|---|---|
| 展示-01 | 缩放范围不一致 | P0 | 已确认 V2.0 为 10%-300%，研发按该范围实现 |
| 展示-02 | 当前无 2D 热力图 | P2 | 不纳入 V2.0 当前实现范围，后续如要做再单独定义颜色映射和图例 |
| 展示-03 | 3D 点图/模型/数字边界不清 | P1 | 拆成独立说明 |
| 展示-04 | 3D 点图颜色映射规则缺失 | P1 | 已明确：支持用户直接设置颜色上限，超上限按最高颜色显示且不改变真实数值 |

### 8.7 验收标准

| 编号 | 验收项 | 通过标准 | 优先级 |
|---|---|---|---|
| 验收-展示-01 | 实时刷新 | 压力变化时视图同步变化 | P0 |
| 验收-展示-02 | 视图切换 | 2D/3D 切换不丢数据、不崩溃 | P0 |
| 验收-展示-03 | 坐垫/靠背切换 | 座椅类设备可切换整体、坐垫、靠背 | P0 |
| 验收-展示-04 | 缩放 | 缩放不影响统计、采集、导出 | P0 |
| 验收-展示-05 | 重置 | 点击重置后恢复默认视角 | P1 |
| 验收-展示-06 | 3D 点图颜色上限 | 用户修改颜色上限后，3D 点图颜色变化，但统计、采集、导出数值不变 | P1 |


### 8.8 PRD1.1 增量补充

#### 8.8.1 2D 数字和 3D 点图显示规则

| 项 | PRD1.1 规则 |
|---|---|
| 2D 数字 | 显示整数压力值，字体白色，不使用黑色描边 |
| 2D 背景色 | 只给背景色做透明度，不影响文字透明度 |
| 3D 点图 | 使用独立点图色阶，颜色更深，高值红色更明显 |
| 颜色调节 | 2D 数字和 3D 点图共用同一个颜色调节 value |
| 0 值显示 | 有效区真实 0 值按低值色显示，补 0 点不展示 |

#### 8.8.2 缩放、视角和旋转中心

| 功能 | PRD1.1 规则 |
|---|---|
| 放大/缩小 | 每次点击固定 10% |
| 百分比显示 | 100% 到 110% 必须表现为放大，不得反向缩小 |
| 视角切换 | 靠背和坐垫都按 0°、向里 45°、向里 90°循环 |
| 整体视图 | 整体模式不显示单对象视角切换入口 |
| 单对象旋转 | 靠背或坐垫单独显示时，左键旋转中心在当前对象上 |
| 整体旋转 | 正常座椅模型、靠背点图、座椅点图同时展示时，旋转中心在整体上 |
| 动画稳定性 | 视图切换和点图切换不能导致 3D 画面突然缩小、跳动或位移 |

#### 8.8.3 PRD1.1 验收标准

| 编号 | 验收项 | 通过标准 |
|---|---|---|
| VIEW-1.1-01 | 2D 数字 | 数字不越出格子，字体为白色且无黑色描边 |
| VIEW-1.1-02 | 3D 色值 | 高值区域能进入黄橙红，整体不发灰 |
| VIEW-1.1-03 | 缩放 | 每次点击固定 10%，首次放大不反向缩小 |
| VIEW-1.1-04 | 视角切换 | 靠背和坐垫均按 0°、45°、90°循环 |
| VIEW-1.1-05 | 旋转中心 | 单对象围绕自身，整体视图围绕整体对象 |

## 10. 模块四：连接之后的功能工具模块

### 10.1 模块目标

| 项目 | 内容 |
|---|---|
| 模块目标 | 提供置零、翻转、框选、量尺、放大镜和可视化调节能力 |
| 核心原则 | 工具可以影响显示和分析；翻转/旋转会修正业务 rawData；框选、颜色、润滑不破坏 rawData |

### 10.2 当前已实现

| 功能 | 当前状态 | 备注 |
|---|---|---|
| 上下翻转 | 已实现 | V1.5 已明确会修正业务 rawData |
| 左右翻转 | 已实现 | V1.5 已明确会修正业务 rawData |
| 预压力置零 | 已实现 | 数据流需补齐 |
| 高斯润滑 | 已实现 | 只影响展示，不影响统计、采集、导出 |
| 颜色调节 | 已实现 | 展示层参数 |
| 噪点消除 | 已实现 | 不影响 rawData，生成后端处理数据流 |
| 高度调节 | 已实现 | 3D 展示 |
| 响应速度调节 | 已实现 | 3D 展示 |
| 恢复默认 | 已实现 | 本地配置恢复 |
| 框选 | 已实现 | 最多 4 个 |
| 手动输入框选坐标 | 已实现 | 有效区域校验 |
| 框选高亮 | 已实现 | 颜色区分 |
| 量尺 | 已实现 | 点距规则需补齐 |
| 放大镜 | 已实现 | 2D 数字视图 |

### 10.3 置零业务逻辑

| 项目 | 业务规则 | 是否待确认 |
|---|---|---|
| 触发入口 | 用户点击预压力置零按钮 | 否 |
| 前置条件 | 设备已连接，当前有有效压力矩阵 | 否 |
| 置零动作 | 将当前压力矩阵记录为基线矩阵 | 否 |
| 后续显示 | 后续帧显示值 = 当前帧 - 基线矩阵，小于 0 的值按 0 处理 | 否 |
| 统计口径 | 统计指标基于置零后矩阵 | 否 |
| 采集口径 | 入库保存置零后的业务矩阵，并记录置零 metadata | 否 |
| 导出口径 | CSV 导出置零后的业务矩阵，并导出 `zero_enabled/zero_time` | 否 |
| 状态记录 | 记录是否置零、置零时间、是否有基线 | 否 |
| 取消置零 | 清空基线，恢复未置零口径 | 否 |

### 10.4 框选业务逻辑

| 项目 | 业务规则 | 优先级 |
|---|---|---|
| 功能定位 | 框选是独立分析模块，不是采集模块的附属功能 | P0 |
| 使用条件 | 仅 2D 数字视图下可编辑框选 | P0 |
| 创建方式 | 鼠标拖拽矩形、手动输入 X/Y/长/宽 | P0 |
| 数量限制 | 最多 4 个框选区域 | P0 |
| 边界校验 | 框选区域不得超出有效矩阵范围 | P0 |
| 未框选 | 显示并统计所有传感点数据 | P0 |
| 已框选 | 显示并统计框选区域数据 | P0 |
| 多框选 | 每个区域独立统计，V2.0 默认不做合并统计 | P1 |
| 采集规则 | 框选不控制采集范围，采集始终保存全量矩阵 | P0 |
| 回放规则 | 回放时可重新创建框选，并基于历史全量矩阵重新计算 | P0 |
| 对比规则 | 数据对比模式下，一个框选区域同时作用于 A/B 和差值图 | P1 |

### 10.4.1 框选与采集解耦原则

| 原则 | 说明 | 优先级 |
|---|---|---|
| 框选是分析模块 | 框选只决定当前分析范围，输出局部统计、局部曲线、局部导出结果 | P0 |
| 采集是保存模块 | 采集保存全量压力矩阵、时间戳、设备信息、采样率、方向等数据 | P0 |
| 框选不决定采集范围 | 无论是否框选，采集都必须保存全量矩阵 | P0 |
| 实时和回放都可框选 | 用户可以在实时模式框选，也可以在历史回放里重新框选 | P0 |
| 回放框选不依赖采集时框选 | 历史回放只要有全量矩阵，就可以新建框选并重新计算区域数据 | P0 |
| 框选结果是派生数据 | 框选统计、框选曲线、框选导出都不是原始采集数据 | P0 |

### 10.4.2 框选坐标和统计口径

| 项目 | 规则 | 优先级 |
|---|---|---|
| 坐标原点 | 当前 2D 数字矩阵左上角 | P0 |
| 坐标单位 | 传感点，不是像素，也不是毫米 | P0 |
| X/Y | X 表示列坐标，Y 表示行坐标 | P0 |
| 长/宽 | 长为横向点数，宽为纵向点数，均必须大于 0 | P0 |
| 最大范围 | X + 长不得超过矩阵列数，Y + 宽不得超过矩阵行数 | P0 |
| 翻转后坐标 | 保存当前显示方向坐标，框选展示、统计、导出以用户当前看到的方向为准 | P0 |
| 平均压强 | 按当前过滤值判断有效点后计算 | P0 |
| 最小压强 | 按当前过滤值判断有效点后计算 | P0 |
| 压力总和 | 按当前过滤值判断有效点后计算 | P0 |
| 压力重心 | 按当前过滤值判断有效点后计算 | P1 |
| 受压面积 | 单位使用 cm²，有效阈值使用当前过滤值 | P0 |
| 正态分布 | V2.0 不做框选正态分布指标 | P2 |

### 10.4.3 框选模板业务逻辑

框选模板是一组可复用的框选区域配置。它用于让用户快速恢复常用分析区域，例如“坐垫左侧/右侧”“靠背肩部/腰部”“前排/后排压力区”。

框选模板不保存压力矩阵，不保存统计结果，不影响采集。模板只保存“要分析哪些区域”。

| 项目 | 规则 | 优先级 |
|---|---|---|
| 模板定位 | 一组框选区域的配置集合 | P1 |
| 模板用途 | 在实时、回放、数据对比中快速应用同一组区域 | P1 |
| 模板内容 | 区域名称、坐标、长宽、颜色、设备类型、矩阵尺寸、显示对象、创建时间 | P1 |
| 不保存内容 | 不保存 rawData、displayData、calculatedData、采集帧、统计结果 | P0 |
| 与采集关系 | 模板不影响采集开始、停止和采集保存范围 | P0 |
| 与历史关系 | 历史记录不强制绑定模板，回放时可临时应用匹配模板 | P1 |
| 与对比关系 | 对比模式中可应用模板，同一区域同时作用于 A/B 和差值图 | P1 |
| 存储方式 | V2.0 建议本地存储，后续可扩展导入/导出模板 | P1 |

### 10.4.4 框选模板主流程

| 步骤 | 用户动作 | 系统处理 | 成功结果 | 异常处理 |
|---|---|---|---|---|
| 1 | 创建框选区域 | 生成当前框选区域列表 | 画布上出现区域 | 越界、尺寸为 0、超过 4 个时不创建 |
| 2 | 点击保存为模板 | 弹出模板保存窗口 | 用户可填写模板名称、备注 | 无区域时按钮置灰 |
| 3 | 保存模板 | 校验名称、设备类型、矩阵尺寸 | 模板进入模板列表 | 名称为空或重复时提示 |
| 4 | 应用模板 | 校验当前设备/历史/对比数据是否匹配 | 模板区域恢复到画布 | 不匹配时禁止应用 |
| 5 | 编辑模板 | 加载模板区域并允许修改 | 保存后更新模板 | 模板不存在时提示 |
| 6 | 删除模板 | 二次确认后删除 | 模板从列表移除 | 已应用区域不受影响 |

### 10.4.5 框选模板匹配规则

推荐：严格匹配。设备类型、显示对象、矩阵尺寸全部一致才可应用。

| 匹配项 | 是否必须匹配 | 说明 | 优先级 |
|---|---|---|---|
| 设备类型 | 是 | 例如汽车坐垫、汽车靠背、endi 坐垫、endi 靠背 | P1 |
| 矩阵尺寸 | 是 | 例如 32x32、46x46、50x64、64x64 | P1 |
| 显示对象 | 是 | 坐垫模板只能用于坐垫，靠背模板只能用于靠背 | P1 |
| 显示方向 | 建议匹配 | 当前按显示方向坐标保存，应用时按当前显示方向恢复 | P1 |
| 置零状态 | 否 | 模板只保存区域，不关心置零 | P1 |
| 采集状态 | 否 | 采集中应用模板只影响当前分析，不影响采集 | P1 |

### 10.4.6 框选模板待确认项和方案

| 待确认项 | 选项一简介 | 选项二简介 | 选项三简介 | 当前采用/推荐 |
|---|---|---|---|---|
| 模板匹配规则 | 设备类型、显示对象、矩阵尺寸严格匹配 | 只要矩阵尺寸一致就允许 | 自动缩放适配不同矩阵 | 当前采用严格匹配 |
| 模板存储位置 | 本地存储 | 存入项目数据库 | 支持云端同步 | V2.0 使用本地存储 |
| 模板是否绑定历史记录 | 不绑定，回放时临时应用 | 历史记录保存模板 ID | 每条历史自动保存当时模板 | 当前采用不绑定历史记录 |
| 应用模板时已有区域 | 弹窗确认后覆盖 | 自动追加到当前区域 | 禁止应用 | 当前采用弹窗确认后覆盖 |
| 模板是否支持导入/导出 | 暂不支持 | 支持 JSON 导入/导出 | 支持云同步 | V2.0 暂不支持，P2 再评估 JSON 导入/导出 |

### 10.5 翻转业务逻辑

画布翻转/旋转用于让用户把传感器实际摆放方向和软件分析方向对齐。当前产品口径确认：翻转/旋转不是单纯显示效果，而是方向修正能力。方向修正后，后续展示、统计、采集、回放、导出都应基于修正后的矩阵。

说明：底层串口原始帧 rawFrame 可保留用于排查；但业务上的 rawData / storedData / exportedData 应按用户确认后的方向保存和使用。

| 项目 | 业务规则 | 优先级 |
|---|---|---|
| 触发入口 | 用户点击左右翻转或上下翻转按钮 | P0 |
| 旋转入口 | 用户点击 90 度旋转按钮；V2.0 采用单按钮，每次点击顺时针旋转 90 度 | P0 |
| 影响对象 | 当前对象的业务矩阵方向，坐垫和靠背分开处理 | P0 |
| 左右翻转 | 当前矩阵按横向镜像修正 | P0 |
| 上下翻转 | 当前矩阵按纵向镜像修正 | P0 |
| 90 度旋转 | 当前矩阵每次按顺时针 90 度旋转，连续点击按 0/90/180/270 循环累加 | P0 |
| 双向翻转 | 同时开启左右和上下翻转时，相当于旋转 180 度 | P0 |
| 坐垫/靠背独立 | 座椅坐垫和靠背分别保存方向状态，互不影响 | P0 |
| 是否影响 rawFrame | 不影响，保留设备原始帧用于排查 | P0 |
| 是否影响 rawData | 影响。业务 rawData 应按修正后的方向生成和保存 | P0 |
| 是否影响 displayData | 影响。展示基于修正后的矩阵 | P0 |
| 是否影响 calculatedData | 影响。统计、坐标、重心基于修正后的矩阵 | P0 |
| 是否影响采集 | 影响。采集保存修正方向后的全量矩阵，并记录方向配置 | P0 |
| 是否影响回放 | 影响。回放直接使用已持久化的修正方向矩阵 | P0 |
| 是否影响 CSV | 影响。CSV 导出修正方向后的矩阵，并导出方向配置字段 | P0 |
| 是否持久化 | 是。方向配置需要按设备类型 + 显示对象保存到本地 | P0 |

### 10.5.1 翻转状态字段

| 状态字段 | 含义 | 示例 | 优先级 |
|---|---|---|---|
| flip_horizontal | 是否左右翻转 | true/false | P0 |
| flip_vertical | 是否上下翻转 | true/false | P0 |
| rotate_degree | 旋转角度 | 0/90/180/270 | P0 |
| rotate_direction | 90 度旋转方向 | V2.0 固定 clockwise | P1 |
| data_direction | 当前方向枚举 | none/horizontal/vertical/both/rotate90/rotate180/rotate270 | P0 |
| display_type | 当前对象 | sit/back/overall | P0 |
| system_type | 当前设备系统 | car-sit、endi-back 等 | P0 |
| persist_key | 持久化键 | car-sit:sit 或 endi-back:back | P0 |
| updated_at | 最后更新时间 | 1710000000000 | P1 |

### 10.5.2 坐垫/靠背独立持久化规则

| 场景 | 规则 | 优先级 |
|---|---|---|
| 汽车坐垫 | 独立保存坐垫方向配置 | P0 |
| 汽车靠背 | 独立保存靠背方向配置 | P0 |
| endi 坐垫 | 独立保存坐垫方向配置 | P0 |
| endi 靠背 | 独立保存靠背方向配置 | P0 |
| 整体视图 | 读取坐垫和靠背各自方向后组合展示 | P0 |
| 切换坐垫/靠背 | 自动加载当前对象的方向配置 | P0 |
| 首次使用 | 无持久化配置时默认为 none/0 度 | P0 |
| 恢复默认 | 只恢复当前对象方向，例如当前坐垫或当前靠背，不影响另一对象 | P1 |

### 10.5.2.1 旋转累加记录规则

| 项目 | 规则 | 优先级 |
|---|---|---|
| 旋转方式 | V2.0 只有一个 90 度旋转按钮，每次点击顺时针旋转 90 度 | P0 |
| 累加状态 | 连续点击按 `0 -> 90 -> 180 -> 270 -> 0` 循环 | P0 |
| 用户多次旋转记录 | 必须记录当前累计角度 `rotate_degree`，而不是只记录最后一次点击 | P0 |
| 坐垫/靠背独立 | 坐垫和靠背分别记录自己的累计旋转角度 | P0 |
| 持久化 | 累计旋转角度写入本地配置，刷新或重启后恢复 | P0 |
| 采集记录 | 开始采集时写入当前累计旋转角度，采集中禁止继续旋转 | P0 |
| CSV 导出 | 导出 `rotate_degree` 和 `data_direction`，用于回放和外部分析 | P0 |

### 10.5.3 翻转与其他模块关系

| 模块 | 影响规则 | 优先级 |
|---|---|---|
| 2D 数字视图 | 数字矩阵按修正后的方向显示 | P0 |
| 3D 点图 | 点位高度和颜色按修正后的矩阵映射 | P1 |
| 3D 模型 | 3D 模型本身不跟随翻转，不镜像模型；压力点是否按修正矩阵贴合需研发确认 | P1 |
| 框选 | 框选坐标基于修正后的矩阵坐标 | P0 |
| 框选模板 | 模板基于修正后的矩阵坐标，应用时需校验方向配置 | P1 |
| 量尺 | 翻转不改变真实距离，只改变显示位置 | P0 |
| 压力重心 | 重心坐标基于修正后的矩阵计算 | P0 |
| 采集 | 保存修正后的业务矩阵，并记录 data_direction/rotate_degree | P0 |
| 回放 | 使用已保存的修正方向矩阵，不再二次翻转 | P0 |
| CSV 导出 | 导出修正后的矩阵，并导出方向配置字段 | P0 |
| 数据对比 | 对比前需要校验方向配置，否则差值图会错位 | P0 |

### 10.5.4 翻转异常处理

| 异常类型 | 判断条件 | 系统处理 | 用户提示 | 优先级 |
|---|---|---|---|---|
| 无数据翻转 | 当前无有效矩阵 | 可切换状态，但显示为空 | 暂无压力数据 | P1 |
| 采集中翻转 | 正在采集时点击翻转/旋转 | 禁止修改方向 | 正在采集，不能修改方向，请停止采集后再操作 | P0 |
| 回放中翻转 | 回放时点击翻转 | 仅影响当前查看，不修改历史数据 | 无 | P1 |
| 数据对比中方向不一致 | A/B 方向不同 | 进入对比前统一或提示 | 两组数据方向不同，请统一后对比 | P0 |
| 方向状态丢失 | 历史记录无 data_direction | 按 none 处理并提示 legacy 数据 | 历史数据缺少方向信息，已按默认方向显示 | P1 |
| 90 度旋转后矩阵尺寸变化 | 例如 50 x 64 旋转后变成 64 x 50 | 更新矩阵宽高并重新校验框选/量尺 | 当前方向已改变，相关区域已重新校验 | P0 |
| 持久化失败 | 本地配置写入失败 | 画面可临时生效，但提示保存失败 | 方向保存失败，下次打开可能不会保留 | P0 |

### 10.6 量尺业务逻辑

量尺用于测量两个传感点或一个区域之间的真实距离。量尺是查看和分析工具，不影响原始数据、采集数据和统计数据。

| 项目 | 业务规则 | 优先级 |
|---|---|---|
| 使用条件 | 仅 2D 数字视图下可用 | P0 |
| 触发入口 | 用户点击量尺按钮进入测量模式 | P1 |
| 互斥规则 | 量尺和框选不能同时启用 | P0 |
| 测量对象 | 两个传感点之间的距离，或矩形区域的宽高距离 | P1 |
| 计算依据 | `pointWidthDistance` 和 `pointHeightDistance`，由软件按设备类型配置维护 | P0 |
| 默认单位 | 推荐使用 cm，必要时展示 mm | P1 |
| 缩放影响 | 缩放只影响显示，不影响真实距离 | P0 |
| 翻转影响 | 翻转只改变显示位置，不改变真实距离 | P0 |
| 采集影响 | 不影响采集，不入库为采集数据 | P0 |
| 回放影响 | 回放中可使用量尺测量历史画面 | P1 |
| 导出影响 | V2.0 不导出量尺结果，后续可支持截图/报告 | P2 |

### 10.6.1 量尺主流程

| 步骤 | 用户动作 | 系统处理 | 成功结果 | 异常处理 |
|---|---|---|---|---|
| 1 | 点击量尺按钮 | 进入量尺模式，鼠标指针变为测量状态 | 可开始测量 | 非 2D 数字视图提示不可用 |
| 2 | 点击或拖拽起点 | 记录起点传感点坐标 | 显示起点 | 起点超出有效矩阵时不记录 |
| 3 | 点击或拖拽终点 | 记录终点传感点坐标 | 显示测量线和距离 | 终点超出有效矩阵时提示 |
| 4 | 查看测量结果 | 根据点距计算横向、纵向、直线距离 | 展示距离数值 | 点距缺失时提示无法计算 |
| 5 | 新增多条量尺 | 保存当前量尺并继续测量 | 画布显示多条量尺 | 超过上限时提示 |
| 6 | 选中量尺 | 高亮当前量尺 | 可删除或查看详情 | 无 |
| 7 | 删除量尺 | 删除当前量尺 | 画布更新 | 删除失败时保持原状 |
| 8 | 退出量尺 | 关闭量尺模式 | 鼠标恢复默认状态 | 当前量尺是否保留按方案处理 |

### 10.6.2 量尺计算规则

| 项目 | 公式/规则 | 优先级 |
|---|---|---|
| 点坐标 | 使用矩阵坐标，不使用 DOM 像素坐标 | P0 |
| 横向距离 | abs(x2 - x1) x pointWidthDistance | P0 |
| 纵向距离 | abs(y2 - y1) x pointHeightDistance | P0 |
| 直线距离 | sqrt(横向距离² + 纵向距离²) | P1 |
| 单位换算 | mm 转 cm 时除以 10 | P1 |
| 缩放后计算 | 仍按矩阵坐标和点距计算，不按屏幕像素计算 | P0 |
| 翻转后计算 | 翻转前后同两点真实距离一致 | P0 |
| 坐垫/靠背切换 | 使用当前 displayType 对应的点距配置 | P0 |
| 不同设备 | 使用当前设备类型对应点距配置 | P0 |

### 10.6.2.1 点距配置来源

| 项目 | 规则 | 优先级 |
|---|---|---|
| 配置来源 | V2.0 由软件按设备类型维护点距配置 | P0 |
| 配置字段 | `pointWidthDistance`、`pointHeightDistance` | P0 |
| 适用范围 | 每个设备类型、坐垫/靠背对象可分别配置 | P0 |
| 单位 | 推荐内部统一使用 mm，界面默认展示 cm | P1 |
| 缺失处理 | 当前设备缺少点距配置时，不显示真实距离并提示“当前设备缺少点距配置，无法计算距离” | P0 |
| 后续扩展 | 预留由硬件/固件返回点距的能力，但不进入 V2.0 | P2 |

### 10.6.3 多量尺管理

| 规则 | 说明 | 优先级 |
|---|---|---|
| 数量上限 | 最多 8 条量尺，避免画布混乱 | P1 |
| 量尺颜色 | 默认使用统一颜色，选中时高亮 | P1 |
| 量尺名称 | 可显示“量尺 1、量尺 2” | P2 |
| 选中高亮 | 当前量尺高亮显示 | P1 |
| 单个删除 | 支持删除选中量尺 | P1 |
| 清空全部 | 支持清空所有量尺 | P1 |
| 与框选互斥 | 开启量尺时如已有框选，提示先关闭框选或自动关闭量尺 | P0 |
| 与放大镜关系 | 量尺模式下建议关闭放大镜，防止鼠标交互冲突 | P1 |

### 10.6.4 量尺异常处理

| 异常类型 | 判断条件 | 系统处理 | 用户提示 | 优先级 |
|---|---|---|---|---|
| 非 2D 数字视图 | 当前不是 2D 数字视图 | 不进入量尺模式 | 请在 2D 数字视图下使用量尺 | P0 |
| 与框选冲突 | 框选工具已开启 | 不进入量尺，或提示关闭框选 | 量尺和框选不能同时使用 | P0 |
| 点距缺失 | 当前设备无点距配置 | 不显示真实距离 | 当前设备缺少点距配置，无法计算距离 | P0 |
| 起点越界 | 起点不在有效矩阵内 | 不记录起点 | 请在有效区域内测量 | P0 |
| 终点越界 | 终点不在有效矩阵内 | 不生成量尺 | 请在有效区域内测量 | P0 |
| 起点终点相同 | 两点相同 | 可显示 0，或提示重新选择 | 测量距离为 0 | P1 |
| 量尺过多 | 达到数量上限 | 不新增 | 最多支持 8 条量尺 | P1 |

### 10.6.5 量尺与翻转待确认项和方案

| 待确认项 | 选项一简介 | 选项二简介 | 选项三简介 | 当前采用/推荐 |
|---|---|---|---|---|
| 采集中翻转是否允许 | 允许，记录方向变更 | 禁止，避免口径变化 | 弹窗确认 | 当前采用采集中禁止翻转/旋转 |
| CSV 是否导出翻转后矩阵 | 导出修正后的业务矩阵 + data_direction | 同时导出设备原始矩阵和修正矩阵 | 只导出设备原始矩阵 | V2.0 导出修正后的业务矩阵；P1 再评估同时导出双矩阵 |
| 回放中翻转是否保存 | 只影响当前查看 | 保存为回放偏好 | 修改历史记录方向 | 当前采用只影响当前查看 |
| 3D 模型是否跟随翻转 | 不跟随，模型本身不镜像 | 跟随压力点映射 | 模型整体镜像 | 当前采用 3D 模型本身不跟随翻转 |
| 框选模板应用是否要求方向一致 | 要求一致 | 不要求，自动换算 | 弹窗询问 | V2.0 要求方向一致 |
| 是否支持 90 度旋转 | 不支持 | 支持顺时针 90 度 | 支持顺/逆时针和 0/90/180/270 | V2.0 使用单按钮顺时针 90 度并记录累计角度 |
| 量尺默认单位 | cm | mm | 同时显示 cm/mm | 当前采用 cm |
| 量尺形态 | 两点直线量尺 | 矩形宽高量尺 | 两者都支持 | V2.0 做两点直线量尺，P1 再做矩形宽高 |
| 多量尺数量上限 | 最多 4 条 | 最多 8 条 | 不限制 | 当前采用最多 8 条 |
| 退出量尺后是否保留 | 当前会话保留 | 退出即清空 | 弹窗询问 | 当前采用当前会话保留 |
| 回放中是否可用量尺 | 可用，只影响查看 | 禁用 | 只暂停时可用 | 当前采用回放中可用 |
| 量尺是否导出 | 不导出 | 导出到报告 | 导出到 CSV | V2.0 不导出，P2 再评估导出到报告 |

### 10.7 可视化调节业务逻辑

| 调节项 | 影响显示 | 影响统计 | 影响采集 | 影响导出 | 待确认 |
|---|---|---|---|---|---|
| 颜色调节 | 是 | 否 | 否 | 否 | 否 |
| 高斯润滑 | 是 | V2.0 不影响 | 否 | 否 | 否 |
| 噪点消除 | 是 | 是 | 是 | 是 | 否 |
| 高度调节 | 是 | 否 | 否 | 否 | 否 |
| 响应速度调节 | 是 | 否 | 否 | 否 | 否 |
| 恢复默认 | 是 | 视具体参数 | 否 | 否 | 是 |

### 10.7.1 置零、滤波和可视化数据流

| 能力 | 影响 rawData | 影响 zeroedData | 影响 displayData | 影响 calculatedData | 影响 storedData | 影响 exportedData | V2.0 结论 |
|---|---|---|---|---|---|---|---|
| 预压力置零 | 不影响 | 影响 | 影响 | 影响 | 记录置零后业务矩阵和置零 metadata | 导出置零后业务矩阵和置零 metadata | 置零是数据基线修正，不是单纯 UI 状态 |
| 取消置零 | 不影响 | 清空置零基线 | 恢复未置零显示 | 恢复未置零统计 | 后续采集按取消后口径保存 | 后续导出按取消后口径输出 | 必须记录置零状态变化 |
| 高斯润滑 | 不影响 | 不影响 | 影响 | 不影响 | 不影响 | 不影响 | V2.0 只作为显示效果 |
| 噪点消除 | 不影响 rawData；生成 backendProcessedData | 影响 | 影响 | 影响 | 记录后端处理后的业务矩阵和处理标记 | 导出后端处理后的业务矩阵和处理标记 | 噪点消除属于后端数据流处理，不覆盖 rawData |
| 颜色调节 | 不影响 | 不影响 | 影响 | 不影响 | 不影响 | 不影响 | 只影响视觉映射 |
| 高度调节 | 不影响 | 不影响 | 影响 3D 高度 | 不影响 | 不影响 | 不影响 | 只影响 3D 视觉 |
| 响应速度调节 | 不影响 | 不影响 | 影响动画/刷新平滑 | 不影响 | 不影响 | 不影响 | 不改变数据值 |

说明：V2.0 为了保证数据可信，颜色、高斯润滑、高度、响应速度只影响前端展示；噪点消除属于后端数据流处理，不覆盖 rawData，但会生成 backendProcessedData，并影响后续展示、统计、采集和导出。CSV 和历史 metadata 必须记录 `noise_removed`。

### 10.8 待补齐项

| 编号 | 问题 | 优先级 | 处理建议 |
|---|---|---|---|
| 工具-01 | 置零数据流不清 | P0 | V1.3 已补 rawData、zeroedData、displayData、storedData 关系，研发按此实现 |
| 工具-02 | 翻转后落库与原始数据原则可能冲突 | P0 | V2.0 采集/导出使用修正后业务矩阵，同时保留 rawFrame 供排查 |
| 工具-03 | 框选采集口径不清 | P0 | 明确全量矩阵始终保存 |
| 工具-04 | 量尺单位缺失 | P1 | 补点距和单位表 |
| 工具-05 | 可视化调节影响范围不清 | P0 | V1.5 已明确：噪点消除走后端处理数据流；颜色、高斯润滑、高度、响应速度只影响展示 |

### 10.9 验收标准

| 编号 | 验收项 | 通过标准 | 优先级 |
|---|---|---|---|
| 验收-工具-01 | 置零 | 置零后显示和统计按置零口径变化 | P0 |
| 验收-工具-02 | 取消置零 | 取消后恢复未置零状态 | P0 |
| 验收-工具-03 | 框选 | 框选后只统计框选区域 | P0 |
| 验收-工具-04 | 框选采集 | 不丢失全量原始矩阵 | P0 |
| 验收-工具-05 | 翻转/旋转 | 支持左右翻转、上下翻转、0/90/180/270 方向切换，业务矩阵方向正确 | P0 |
| 验收-工具-06 | 坐垫/靠背方向独立 | 坐垫和靠背方向配置互不影响 | P0 |
| 验收-工具-07 | 方向持久化 | 重新打开软件后恢复上次保存的坐垫/靠背方向 | P0 |
| 验收-工具-08 | 采集中禁止翻转 | 采集中点击翻转/旋转被禁止并提示 | P0 |
| 验收-工具-09 | 量尺 | 缩放和翻转后仍按真实距离计算 | P1 |
| 验收-工具-10 | 多量尺 | 最多支持 8 条量尺，第 9 条禁止新增并提示 | P1 |


### 10.10 PRD1.1 增量补充

#### 10.10.1 置零采集口径

| 项 | PRD1.1 规则 |
|---|---|
| 前端置零 | 用户点击置零后同步当前基线到后端 |
| 后端采集 | 采集入库前按基线扣减压力值 |
| 导出记录 | CSV 中记录 `zero_enabled`、`zero_time`、`zero_state` |
| 数据边界 | 置零影响后端采集口径，不只是前端显示效果 |

#### 10.10.2 框选解耦和模板

| 项 | PRD1.1 规则 |
|---|---|
| 框选入口 | 使用顶部工具栏小浮窗，不使用右侧抽屉作为主入口 |
| 有效区 | 框选必须落在真实矩阵有效区，靠背按 50 x 64 和 mask 判断 |
| 无效提示 | 在无效区域框选时提示“请在有效区域框选” |
| 采集解耦 | 框选只影响显示和统计，不影响采集全量矩阵 |
| 回放解耦 | 回放中可以继续框选，框选不暂停回放 |
| 历史解耦 | 历史记录不自动显示“已框选”，也不自动套用旧框选 |
| 模板保存 | 保存区域名称、坐标、颜色、设备 key、显示对象、矩阵尺寸，不保存压力数据 |
| 模板兼容 | 兼容旧 localStorage key 和旧字段结构，并归并到稳定 key |
| 模板重命名 | 选择模板后可重命名并持久化 |
| 退出提示 | 未保存的新框选退出时提示是否保存为模板；已来自模板的框选不重复提示 |

#### 10.10.3 框选快捷键保护

| 场景 | PRD1.1 规则 |
|---|---|
| 输入框聚焦 | Delete、Backspace、方向键只作用于输入框文本 |
| 弹窗/Popover 内 | 不响应框选快捷键 |
| 非输入态 | 允许方向键微调框选，Delete/Backspace 删除选中框选 |

#### 10.10.4 翻转和方向持久化

| 项 | PRD1.1 规则 |
|---|---|
| 坐垫 | 支持上下翻转、左右翻转、90 度旋转 |
| 靠背 | 支持左右翻转 |
| 数据影响 | 翻转改变业务数据源并持久化，不只是视觉层变化 |
| 方向结构 | 方向状态支持 `byKey`、`rotateDegree`、`data_direction`，坐垫/靠背独立记录 |
| 采集中 | 禁止翻转/旋转，提示停止采集后再操作 |
| 回放中 | 根据历史帧方向和当前显示方向差异渲染，避免二次翻转 |

#### 10.10.5 可视化调节补充

| 参数 | PRD1.1 规则 |
|---|---|
| 图像润滑 | 最小 0.1，步进 0.1，最大默认 4 |
| 颜色调节 | 最大默认 500，2D/3D 共用同一个 color value |
| 噪点消除 | 最大默认 20，低于阈值的点对象仍存在 |
| 高度调节 | 最小 0.1，真实值最大 10，slider 显示按真实值 * 10 |
| 响应速度 | 去掉该调节项 |
| 本地持久化 | 切换页面、刷新、切换视图后保留上次值和 Max 设置 |

#### 10.10.6 PRD1.1 验收标准

| 编号 | 验收项 | 通过标准 |
|---|---|---|
| TOOL-1.1-01 | 框选采集解耦 | 有框选时采集仍保存全量矩阵 |
| TOOL-1.1-02 | 模板持久化 | 刷新后模板不丢失，旧模板可兼容显示 |
| TOOL-1.1-03 | 快捷键保护 | 输入框中删除文字不会删除框选 |
| TOOL-1.1-04 | 翻转持久化 | 刷新和采集后仍按当前方向处理数据 |
| TOOL-1.1-05 | 可视化持久化 | 切换页面后保留调节值和 Max 值 |
| TOOL-1.1-06 | 置零采集 | 采集和导出都能看到置零口径 metadata |

## 11. 模块五：数据可视化和计算数据

### 11.1 模块目标

| 项目 | 内容 |
|---|---|
| 模块目标 | 把压力矩阵转换成可理解的指标和趋势图 |
| 核心要求 | 指标公式、单位、是否排除 0 必须明确 |
| 统计范围 | 支持全量和框选区域 |

### 11.2 当前已实现

| 功能 | 当前状态 | 待补充 |
|---|---|---|
| 平均压强 | 已实现 | 公式、单位 |
| 最大压强 | 已实现 | 坐标规则 |
| 最小压强 | 已实现 | 是否排除 0 |
| 压力总和 | 已实现 | 单位 |
| 受压面积 | 已实现 | 有效阈值使用当前过滤值，点距按设备类型配置 |
| 传感点数 | 已实现 | 总点数/有效点数 |
| 压力重心 | 已实现 | 坐标原点和公式 |
| 正态分布指标 | 已实现 | 统计窗口 |
| 压力曲线 | 已实现 | 实时滚动显示最近 30 帧 |
| 面积曲线 | 已实现 | 实时滚动显示最近 30 帧 |
| 压力重心轨迹 | 已实现 | 实时滚动显示最近 30 帧，回放同步完整历史帧 |
| 压力正态分布图 | 已实现 | 统计对象 |

### 11.3 指标业务逻辑

| 指标 | 业务含义 | 计算范围 | 是否排除 0 | 单位 | V2.0 规则 |
|---|---|---|---|---|---|
| 平均压强 | 当前受压区域的平均压力水平 | 全量或框选 | 是 | 当前软件压力单位 | `有效压力点求和 / 有效点数` |
| 最大压强 | 当前帧压力最高点 | 全量或框选 | 否 | 当前软件压力单位 | 取范围内最大值，并记录坐标 |
| 最小压强 | 当前帧非零最小压力 | 全量或框选 | 是 | 当前软件压力单位 | 无有效点时显示 0 或空值 |
| 压力总和 | 有效压力点求和 | 全量或框选 | 是 | 当前软件压力单位求和 | 只统计达到当前过滤值的点 |
| 受压面积 | 达到当前过滤值的受压区域面积 | 全量或框选 | 是 | cm² | `有效点数 x 单点面积`，有效点阈值使用当前过滤值 |
| 传感点数 | 参与统计的点数量 | 全量或框选 | 分总点/有效点 | 个 | 有效点按当前过滤值判断，同时保留总点数和有效点数 |
| 压力重心 | 压力分布中心 | 全量或框选 | 是 | 矩阵坐标 | 以压力值为权重计算 x/y |
| 正态分布 | 压力分布特征 | 当前帧或时间窗口 | 是 | 统计量 | V2.0 不作为发布阻塞，后续单独定义 |

#### 11.3.1 当前过滤值定义

| 项目 | V2.0 规则 | 说明 |
|---|---|---|
| 当前过滤值来源 | 使用用户在可视化/数据处理设置中的过滤值 | 用于判断有效点、受压面积、有效点数、压力总和等指标 |
| 未设置过滤值 | 默认按大于 0 判断有效点 | 保证旧数据和默认配置可用 |
| 修改过滤值后 | 实时统计、框选统计、图表随下一帧刷新 | 不修改 rawData，只影响有效点判断和派生统计 |
| 回放中修改过滤值 | 当前帧和图表按新过滤值重新计算 | 不覆盖历史原始数据 |

### 11.4 框选统计业务逻辑

| 状态 | 统计范围 | 图表范围 | CSV 范围 | 备注 |
|---|---|---|---|---|
| 未框选 | 全量矩阵 | 全量曲线 | 全量矩阵 | 默认状态 |
| 单框选 | 单个框选区域 | 框选区域曲线 | 全量 + 框选派生数据 | 不丢失全量 |
| 多框选 | 每个区域独立统计 | 每个区域独立曲线 | 全量 + 多区域派生数据 | 不做合并曲线 |
| 取消框选 | 恢复全量 | 恢复全量曲线 | 全量矩阵 | 清空区域统计 |

### 11.5 图表业务逻辑

| 图表 | 数据来源 | 实时更新规则 | 回放逻辑 | 业务规则 |
|---|---|---|---|---|
| 压力总和曲线 | 每帧压力总和 | 滚动显示最近 30 帧 | 随回放进度同步，历史回放可使用完整帧 | 与当前过滤值一致 |
| 面积变化曲线 | 每帧受压面积 | 滚动显示最近 30 帧 | 随回放进度同步，历史回放可使用完整帧 | 受压面积有效阈值使用当前过滤值 |
| 压力重心轨迹 | 每帧压力重心 | 滚动显示最近 30 帧 | 随回放进度同步，历史回放可使用完整帧 | 坐标基于修正方向后的矩阵 |
| 正态分布图 | 当前帧或最近 30 帧窗口 | 实时更新 | 随回放帧更新 | V2.0 不作为发布阻塞 |
| 框选区域曲线 | 框选区域统计 | 每个框选区域独立曲线，滚动显示最近 30 帧 | 基于历史全量矩阵重算 | 多框选不合并曲线 |

### 11.6 carY 分压业务逻辑

| 项目 | V2.0 规则 | 说明 |
|---|---|---|
| 特殊规则 | carY 系统存在除以 `100 / 3` 的业务换算 | 当前按代码既有规则延续 |
| 规则定位 | 属于 carY 的业务换算规则，不作为通用压力算法 | 避免影响其他设备类型 |
| 是否影响 rawFrame | 不影响 | rawFrame 保留设备原始帧 |
| 是否影响 rawData | 不影响设备原始矩阵定义；如当前实现已保存换算值，CSV 必须标注 | V2.0 先保证字段可解释 |
| 是否影响 calculatedData | 影响 | 平均、最大、最小、总和、面积等统计使用换算后的业务值 |
| 是否影响 displayData | 影响 | carY 展示应与用户看到的统计一致 |
| 是否影响 storedData | 影响 | 入库保存业务可回放矩阵，并记录 `pressure_conversion=carY_100_div_3` |
| 是否影响 CSV | 影响 | CSV metadata 必须写入转换规则，避免用户误解 |
| QA 验收 | 使用同一帧原始样例，验证 carY 展示值、统计值、导出值一致 | 不要求 QA 解释硬件原理，只验证软件口径一致 |

### 11.6.1 carY 导出说明字段

| 字段 | 含义 | 是否必须 | 示例 |
|---|---|---|---|
| `pressure_conversion` | 压力换算规则 | carY 必须 | `carY_100_div_3` |
| `pressure_conversion_desc` | 换算说明 | carY 建议 | `carY 数据按 100/3 规则换算后用于展示、统计、采集和导出` |
| `pressure_unit` | 压力单位 | 必须 | `software_unit` 或后续标准单位 |

### 11.7 待补齐项

| 编号 | 问题 | 优先级 | 处理建议 |
|---|---|---|---|
| 数据-01 | 统计公式缺失 | P0 | 新建数据字典 |
| 数据-02 | 单位缺失 | P0 | 明确压力、面积、点距单位 |
| 数据-03 | 是否排除 0 不清 | P0 | 每个指标单独定义 |
| 数据-04 | carY 分压规则缺少解释 | P0 | 硬件/算法确认 |
| 数据-05 | 图表缓存和采样频率不清 | P1 | 已明确：实时图表滚动显示最近 30 帧，回放和导出使用完整帧 |

### 11.8 验收标准

| 编号 | 验收项 | 通过标准 | 优先级 |
|---|---|---|---|
| 验收-数据-01 | 实时统计 | 指标随压力变化实时更新 | P0 |
| 验收-数据-02 | 指标口径 | 每个指标有公式、单位、是否排除 0 | P0 |
| 验收-数据-03 | 框选统计 | 框选后统计区域正确 | P0 |
| 验收-数据-04 | 图表一致 | 图表数据和统计数据一致 | P1 |
| 验收-数据-05 | carY | QA 可按规则验证 carY 计算 | P0 |
| 验收-数据-06 | 图表缓存 | 实时曲线只保留最近 30 帧可见数据，回放和导出不丢完整帧 | P1 |


### 11.9 PRD1.1 增量补充

#### 11.9.1 框选统计和图表口径

| 项 | PRD1.1 规则 |
|---|---|
| 有框选时 | 左侧压力曲线、面积曲线只展示框选区域数据 |
| 无框选时 | 继续展示坐垫/靠背整体数据 |
| 多框选 | 最多支持 4 个框选区域，指标行按可用空间分列 |
| 图例 | 框选名称和颜色进入图表图例，便于区分区域 |
| y 轴范围 | 有框选时只按框选曲线计算 y 轴范围 |

#### 11.9.2 框选重心

| 项 | PRD1.1 规则 |
|---|---|
| 计算方式 | 先计算框内局部重心，再投影回整张矩阵坐标 |
| 展示方式 | 压力中心图按框选颜色展示多个重心点 |
| 无框选 | 保留原靠背/座椅整体重心展示方式 |

#### 11.9.3 色值和数字显示

| 项 | PRD1.1 规则 |
|---|---|
| 2D 数字 | 数值保持整数，颜色只作用于背景，文字始终不透明 |
| 3D 点图 | 使用独立色阶，颜色更深，高值红色更明显 |
| 颜色调节 | 2D 和 3D 共用同一个颜色上限 value |
| 0 值 | 有效区 0 值显示低值色，补 0 点隐藏 |

#### 11.9.4 carY 分压和导出说明

| 项 | PRD1.1 规则 |
|---|---|
| carY 换算 | 继续按当前业务口径处理 |
| 导出说明 | CSV 中增加 `pressure_conversion=carY_100_div_3` 和说明字段 |
| 文档说明 | 数据字典和导出说明需明确该换算口径 |

#### 11.9.5 PRD1.1 验收标准

| 编号 | 验收项 | 通过标准 |
|---|---|---|
| CHART-1.1-01 | 框选图表 | 有框选时左侧图表只展示框选数据 |
| CHART-1.1-02 | 多框选布局 | 四个框选时指标数字和单位不换行 |
| CHART-1.1-03 | 框选重心 | 压力中心图展示框选区域重心 |
| CHART-1.1-04 | 色值一致 | 2D/3D 共用颜色调节 value |
| CHART-1.1-05 | carY 导出 | CSV 能说明 carY 换算口径 |

## 12. 模块六：采集、历史数据与回放

### 12.1 模块目标

| 项目 | 内容 |
|---|---|
| 模块目标 | 采集压力数据，保存历史，并支持回放、管理和异常恢复 |
| 核心要求 | 采集中不能丢数据，异常退出后可恢复或保留缓存 |

### 12.2 当前已实现

| 功能 | 当前状态 | 备注 |
|---|---|---|
| 开始采集 | 已实现 | 按钮变停止 |
| 停止采集 | 已实现 | 按钮变采集 |
| 数据命名 | 已实现 | 支持编辑 |
| 备注 | 已实现 | remarks 表 |
| 采集频率 | 已实现 | 后端按帧时间计算 HZ，硬件返回值记录校验 |
| 框选采集 | 已实现 | 全量保存需确认 |
| 翻转方向保存 | 已实现 | rawData 口径需确认 |
| 历史列表 | 已实现 | 支持浏览 |
| 搜索 | 已实现 | 需 QA |
| 重命名 | 已实现 | 需 QA |
| 删除 | 已实现 | 需 QA |
| 回放 | 已实现 | 播放、暂停、倍速、进度条 |
| 框选回放 | 已实现 | 需确认是否基于全量矩阵重算 |
| 图表同步 | 已实现 | 实时图表滚动最近 30 帧，回放使用完整历史帧 |
| 本地缓存 | 已实现 | 历史数据定义已补充，清理规则仍需确认 |

### 12.3 采集业务逻辑

| 场景 | 前置条件 | 用户动作 | 系统处理 | 成功结果 | 异常处理 |
|---|---|---|---|---|---|
| 开始采集 | 设备在线、授权通过、类型匹配 | 点击采集 | 创建采集任务，记录名称、备注、框选、方向 | 按钮变停止，开始写库 | 校验失败拒绝采集 |
| 采集中 | 正在采集 | 无 | 每帧写入 SQLite | 历史数据持续增加 | 写入失败记录缓存并提示 |
| 停止采集 | 正在采集 | 点击停止 | 停止写入，生成历史记录 | 历史列表出现记录 | 停止失败保留缓存 |
| 框选采集 | 存在框选区域 | 点击采集 | 保存全量矩阵和框选元数据 | 后续可重算框选统计 | 不得只保存框选矩阵 |
| 翻转采集 | 用户调整方向 | 点击采集 | 保存修正方向后的业务矩阵和方向 metadata | 回放时按已保存方向直接展示 | 采集中禁止修改方向 |

### 12.4 历史数据业务逻辑

| 字段 | 业务含义 | 是否必须 | 当前状态 | 备注 |
|---|---|---|---|---|
| 记录 ID | 唯一标识一条采集记录 | 是 | 已有类似字段 | 建议标准化 |
| 记录名称 | 用户识别数据用途 | 是 | 已实现 | 可重命名 |
| 备注 | 补充实验信息 | 否 | 已实现 | remarks 表 |
| 开始时间 | 采集开始时间 | 是 | 必须入库 | 用于回放和追溯 |
| 结束时间 | 采集结束时间 | 是 | 必须入库 | 异常中断时写入恢复时间或中断时间 |
| 设备 MAC | 设备身份 | 是 | 必须入库 | 必须可追溯 |
| 设备类型 | 坐垫/靠背等 | 是 | 必须入库 | 必须可追溯 |
| 固件版本 | 设备版本 | 建议 | 待补齐 | 科研/售后需要 |
| 采样率 | 后端计算的实际 HZ | 是 | 部分实现 | 回放和 CSV 使用主采样率 |
| 硬件返回采样率 | 设备上报 HZ | 建议 | 待补齐 | 仅用于记录和校验 |
| 是否置零 | 数据口径 | 是 | 必须入库 | 影响统计可信 |
| 置零时间 | 数据口径 | 是 | 必须入库 | 未置零时为空 |
| 翻转方向 | 展示方向 | 是 | 必须入库 | 回放直接使用已保存业务矩阵，不二次翻转 |
| 全量矩阵 | 原始压力数据 | 是 | 已实现 | 不得丢失 |
| 框选区域 | 区域元数据 | 否 | 已实现 | 派生统计 |

### 12.5 回放业务逻辑

| 操作 | 前置条件 | 系统处理 | 结果 | 边界规则 |
|---|---|---|---|---|
| 播放 | 已加载历史数据 | 按帧推进 | 视图、统计、图表同步更新 | 到最后一帧自动停止 |
| 暂停 | 正在播放 | 停止推进帧 | 停在当前帧 | 再次播放继续 |
| 拖动进度条 | 已加载历史数据 | 跳转到指定 index | 刷新当前帧 | index 越界需归一 |
| 倍速 | 正在回放 | 调整帧间隔或跳帧策略 | 播放速度变化 | 支持 0.5x、1x、2x、4x，默认 1x |
| 框选回放 | 历史全量矩阵存在 | 根据框选区域重算 | 生成区域统计曲线 | 不依赖旧 selectData |
| 退出回放 | 正在回放 | 清理回放状态 | 返回历史或实时界面 | 不删除历史数据 |

### 12.5.1 回放倍速规则

| 项目 | V2.0 规则 | 说明 |
|---|---|---|
| 默认倍速 | 1x | 进入回放默认按 1x 播放 |
| 支持倍速 | 0.5x、1x、2x、4x | 使用固定选项，不做用户自定义输入 |
| 倍速生效方式 | 优先调整帧间隔 | 不应改变历史帧原始数据 |
| 高倍速边界 | 4x 下如渲染跟不上，可按时间推进到最近帧 | 但进度条、当前帧 index、图表必须一致 |
| 暂停后恢复 | 保留当前倍速 | 用户切换历史记录后恢复默认 1x |

### 12.5.2 回放损坏帧处理

| 场景 | 判定条件 | 系统处理 | 用户提示 | 日志/统计 | 优先级 |
|---|---|---|---|---|---|
| 单帧矩阵为空 | 当前帧缺少 real_data 或矩阵为空 | 跳过该帧，播放下一帧 | 不弹窗，可在回放控件显示“已跳过异常帧” | skipped_frame_count +1 | P0 |
| 单帧矩阵长度不匹配 | 当前帧点数不等于历史记录矩阵配置 | 跳过该帧 | 当前数据存在异常帧，已自动跳过 | 记录 frame_index、期望点数、实际点数 | P0 |
| 单帧数值非法 | 出现非数字、无法解析 JSON、负值是否非法按设备规则判断 | 跳过该帧 | 当前数据存在异常帧，已自动跳过 | 记录解析错误 | P0 |
| 连续损坏帧 | 连续 10 帧损坏 | 暂停回放，允许用户继续或退出 | 历史数据损坏较多，建议检查文件 | 记录连续损坏区间 | P0 |
| 全部帧不可用 | 可用帧数为 0 | 阻止进入回放 | 当前历史数据已损坏，无法回放 | 标记记录状态为异常 | P0 |
| 框选回放越界 | 历史矩阵尺寸与框选区域不匹配 | 忽略该框选或重新校验 | 框选区域超出历史数据范围，已取消该区域 | 记录 selection_id | P1 |

说明：回放遇到损坏帧时，不应让整个软件崩溃。V2.0 默认“能跳过就跳过，连续异常才暂停”，并在回放结束后给出异常帧数量。

### 12.6 异常恢复业务逻辑

| 异常 | 系统识别方式 | 系统处理 | 用户选择 | 结果 |
|---|---|---|---|---|
| 采集中强退 | 存在未完成采集状态 | 启动时检查缓存/数据库 | 恢复或丢弃 | 生成恢复记录或保留缓存 |
| 设备断开 | 实时数据中断或串口关闭 | 停止采集或进入异常状态 | 重新连接 | 保留已采集数据 |
| 数据库损坏 | SQLite quick_check 失败 | 备份损坏库并重建 | 提示用户 | 避免覆盖原文件 |
| 写入失败 | 写库异常 | 记录日志并提示 | 重试或停止 | 保留已有数据 |
| 导出失败 | 文件写入失败 | 提示失败原因 | 重新选择路径 | 不影响历史数据 |

### 12.6.1 采集中强退恢复流程

V2.0 采用默认推荐恢复策略：如果存在有效帧，默认推荐“恢复为异常中断历史记录”；只有用户明确确认丢弃时，才删除临时任务。

| 步骤 | 系统动作 | 判断结果 | 用户选择 | 处理规则 | 优先级 |
|---|---|---|---|---|---|
| 1 | 软件启动时检查采集状态 | 存在 `collecting=true` 或未完成记录 | 无 | 进入异常恢复状态，不自动覆盖 | P0 |
| 2 | 检查临时缓存和数据库帧数 | 有可恢复帧 | 恢复为历史记录 | 生成一条“异常中断”历史记录 | P0 |
| 3 | 检查临时缓存和数据库帧数 | 无有效帧 | 丢弃 | 清理空任务并记录日志 | P0 |
| 4 | 用户选择恢复 | 有效帧数大于 0 | 恢复 | 补结束时间、异常结束标记、帧数、metadata | P0 |
| 5 | 用户选择暂不处理 | 有可恢复帧 | 稍后处理 | 保留缓存，不允许新采集覆盖同一缓存 | P1 |
| 6 | 用户选择丢弃 | 用户确认二次弹窗 | 丢弃 | 删除临时任务，保留异常日志 | P0 |
| 7 | 恢复失败 | 数据库写入失败或缓存损坏 | 重试/导出日志 | 不删除原缓存，提示联系售后 | P0 |

### 12.6.2 异常中断历史记录字段

| 字段 | 含义 | 是否必须 |
|---|---|---|
| `record_status` | 历史记录状态：normal/interrupted/recovered/corrupted | 是 |
| `interrupted_at` | 异常中断时间 | 是 |
| `recovered_at` | 恢复时间 | 恢复时必须 |
| `frame_count_before_interrupt` | 强退前已保存帧数 | 是 |
| `recover_source` | 恢复来源：database/cache/both | 是 |
| `recover_note` | 恢复说明 | 建议 |

### 12.6.3 历史数据与本地缓存定义

| 类型 | 定义 | 是否允许自动清理 | 说明 |
|---|---|---|---|
| 历史数据 | 用户点击采集后形成的传感器压力记录，包括全量压力帧、时间戳、设备信息、方向、置零状态、框选元数据、名称和备注 | 待确认，当前默认不自动删除 | 这是用户实验或演示结果，不等同于普通缓存 |
| 临时采集缓存 | 采集中断、写库失败或强退恢复前保存的临时帧和任务状态 | 待确认 | 用于防丢失，恢复后可转成历史数据 |
| 设备配置缓存 | MAC、设备类型、系统类型、端口偏好、可视化配置等 | 可手动清理 | 清理后可能需要重新配置设备 |
| UI/运行缓存 | 搜索条件、窗口状态、最近打开路径等 | 可手动清理 | 不应影响历史采集记录 |

说明：DEC-19 目前只明确“历史数据”含义。V2.0 默认不自动删除历史数据；是否对临时采集缓存做超过 30 天提示清理，仍需产品确认。

### 12.7 待补齐项

| 编号 | 问题 | 优先级 | 处理建议 |
|---|---|---|---|
| 采集-01 | 历史字段不完整 | P0 | 补历史数据结构 |
| 采集-02 | 异常恢复流程不完整 | P0 | 补未完成采集识别和恢复流程 |
| 采集-03 | 采集中系统切换限制不清 | P1 | 前后端都禁止 |
| 采集-04 | 回放边界规则缺失 | P1 | 已补倍速范围、暂停恢复、高倍速边界；仍需研发确认具体跳帧实现 |
| 采集-05 | 缓存清理规则缺失 | P1 | 已补历史数据和缓存定义；自动清理策略仍需确认 |

### 12.8 验收标准

| 编号 | 验收项 | 通过标准 | 优先级 |
|---|---|---|---|
| 验收-采集-01 | 开始/停止 | 按钮状态正确切换 | P0 |
| 验收-采集-02 | 历史生成 | 停止后生成历史记录 | P0 |
| 验收-采集-03 | 历史管理 | 可搜索、重命名、备注、删除 | P1 |
| 验收-采集-04 | 回放 | 支持播放、暂停、拖动、0.5x/1x/2x/4x 倍速，默认 1x | P0 |
| 验收-采集-05 | 回放同步 | 视图、统计、图表同步变化 | P0 |
| 验收-采集-06 | 异常恢复 | 强退后可恢复或保留数据 | P0 |
| 验收-采集-07 | 历史数据保护 | 清理缓存时不自动删除用户采集形成的历史数据 | P0 |


### 12.9 PRD1.1 增量补充

#### 12.9.1 采集补充

| 项 | PRD1.1 规则 |
|---|---|
| 采集范围 | 始终采集全量矩阵，不被当前框选影响 |
| 方向 | 采集保存当前业务方向后的矩阵 |
| 置零 | 采集保存置零后的业务矩阵，并记录置零 metadata |
| 异常帧 | `device_error/matrix_error/offline` 等异常帧不得进入采集入库 |
| 采集文字 | “数据采集”字号与“历史数据”入口一致 |

#### 12.9.2 历史数据补充

| 项 | PRD1.1 规则 |
|---|---|
| 页签切换 | 本地数据、导入数据、导出数据切换不复制列表项 |
| 选中态 | 切换页签时清理编辑、删除、下载、对比选中态 |
| 框选标记 | 历史卡片不自动显示“已框选”状态 |
| 打开路径 | 点击“打开”可以打开文件夹，失败时提示原因 |
| 性能 | 历史列表分批渲染，滚动接近底部时加载更多 |

#### 12.9.3 回放补充

| 项 | PRD1.1 规则 |
|---|---|
| 倍速 | 只允许 0.5x、1x、2x、4x，非法倍速回退 1x |
| 损坏帧 | 空帧、矩阵缺失、长度不匹配、非法数值自动跳过 |
| 连续异常 | 连续异常达到阈值时停止回放并提示 |
| 回放框选 | 正常播放和拖动进度条都按当前框选范围展示 |
| 回放图表 | 优先使用历史接口返回的压力/面积曲线，不依赖实时帧 |
| 退出回放 | 如果设备仍连接，状态恢复为已连接 |

#### 12.9.4 PRD1.1 验收标准

| 编号 | 验收项 | 通过标准 |
|---|---|---|
| PLAY-1.1-01 | 全量采集 | 有框选时采集仍保存全量数据 |
| PLAY-1.1-02 | 页签切换 | 本地/导入/导出数据切换不重复追加 |
| PLAY-1.1-03 | 回放框选 | 播放和拖动进度条都只展示框选数据 |
| PLAY-1.1-04 | 损坏帧 | 损坏帧跳过，连续异常停止并提示 |
| PLAY-1.1-05 | 退出状态 | 退出回放后设备在线则显示已连接 |
| PLAY-1.1-06 | 历史性能 | 大量历史记录打开和滚动不卡死 |

## 13. 模块七：数据导出与数据对比

### 13.1 模块目标

| 项目 | 内容 |
|---|---|
| 模块目标 | 历史数据可稳定导出 CSV，并支持本地 CSV 导入校验 |
| 后续目标 | 支持双组数据对比、红蓝差值图和分区压力分析 |

### 13.2 当前已实现

| 功能 | 当前状态 | 备注 |
|---|---|---|
| CSV 下载 | 已实现 | 当前接口 `/downlaod` |
| 路径选择 | 已实现 | Electron API |
| 打开文件/文件夹 | 已实现 | 导出后可打开 |
| 下载进度提示 | 已实现 | 需 QA |
| CSV 导入 | 已实现 | 上传校验 |
| 数据结构校验 | 已实现 | 不符合提示“数据有误” |
| 数据对比接口/组件 | 部分实现 | 需独立说明 |

### 13.3 CSV 导出业务逻辑

| 场景 | 前置条件 | 用户动作 | 系统处理 | 输出 | 异常处理 |
|---|---|---|---|---|---|
| 导出历史数据 | 存在历史记录 | 点击下载 | 选择路径，读取历史帧，生成 CSV | CSV 文件 | 失败提示原因 |
| 打开文件 | 导出成功 | 点击打开文件 | 调用系统打开路径 | 打开 CSV | 文件不存在时提示 |
| 打开文件夹 | 导出成功 | 点击打开文件夹 | 调用系统打开目录 | 打开目录 | 路径不存在时提示 |
| 导出框选数据 | 历史记录包含框选 | 点击下载 | 导出全量数据和框选派生数据 | CSV 文件 | 不得只导出框选数据 |

### 13.4 CSV 导入业务逻辑

| 场景 | 前置条件 | 用户动作 | 系统处理 | 成功结果 | 失败处理 |
|---|---|---|---|---|---|
| 导入 CSV | 用户选择文件 | 上传 CSV | 校验文件后缀、基础字段、矩阵字段、矩阵长度 | 加入本地导入列表 | 提示“数据有误” |
| 加载导入数据 | CSV 已导入 | 选择数据 | 读取 CSV 并解析成历史帧 | 可回放或查看 | 解析失败提示 |
| 兼容旧格式 | CSV 无版本号 | 上传 CSV | 按 legacy 规则解析 | 成功导入 | 不兼容提示版本问题 |
| 进入对比 | CSV 导入成功 | 选择对比 | 作为对比数据源 | 进入对比视图 | 数据结构不一致时提示 |

### 13.5 CSV 文件头字段

| 字段 | 业务含义 | 是否必须 | 当前状态 | 优先级 |
|---|---|---|---|---|
| `csv_format_version` | CSV 格式版本 | 是 | 待补齐 | P0 |
| `software_version` | 软件版本 | 是 | 待补齐 | P0 |
| `device_mac` | 设备 MAC | 是 | 待补齐 | P0 |
| `device_type` | 设备类型 | 是 | 待补齐 | P0 |
| `system_type` | 系统类型 | 是 | 待补齐 | P0 |
| `firmware_version` | 固件版本 | 是 | 硬件返回，软件读取 | P1 |
| `sample_rate_hz` | 后端计算采样率 | 是 | 按帧时间计算 | P0 |
| `hardware_sample_rate_hz` | 硬件返回采样率 | 建议 | 用于记录和校验 | P1 |
| `baud_rate` | 波特率 | 建议 | 待补齐 | P1 |
| `zero_enabled` | 是否置零 | 是 | 待补齐 | P0 |
| `zero_time` | 置零时间 | 是 | 待补齐 | P0 |
| `data_direction` | 翻转/旋转方向 | 是 | 需包含累计旋转角度 | P0 |
| `noise_removed` | 是否启用噪点消除 | 是 | 后端处理标记 | P0 |
| `created_at` | 导出时间 | 是 | 待补齐 | P0 |

### 13.6 CSV 数据区字段

| 标准字段 | 当前实现字段参考 | 业务含义 | 是否必须 | 优先级 |
|---|---|---|---|---|
| `frame_index` | 行号或待新增 | 帧序号 | 是 | P0 |
| `timestamp` | `time` / `sec(s)` | 时间戳或秒数 | 是 | P0 |
| `avg_pressure` | `{key}aver` | 平均压强 | 是 | P0 |
| `max_pressure` | `{key}max` | 最大压强 | 是 | P0 |
| `max_pressure_coord` | `{key}maxCoord` | 最大值坐标，格式建议为 `row,col` 或 `{row,column}` | 是 | P0 |
| `min_pressure_non_zero` | 待新增 | 非零最小压强 | 是 | P0 |
| `pressure_sum` | `{key}pressTotal` | 压力总和 | 是 | P0 |
| `contact_area` | `{key}pressureArea` | 受压面积 | 是 | P0 |
| `active_sensor_count` | `{key}point` | 有效传感点数 | 是 | P0 |
| `real_data` | `{key}realData` | 全量压力矩阵 | 是 | P0 |
| `select_data` | `{key}selectData` | 框选区域派生数据 | 否 | P1 |

### 13.6.1 框选 CSV 数据区字段

当采集或导出时存在框选区域，CSV 除全量字段外，还需要导出每个框选区域的统计字段。字段命名建议使用 `selection_{n}_字段名`，其中 `n` 从 1 开始。

| 标准字段 | 业务含义 | 是否必须 | 优先级 |
|---|---|---|---|
| `selection_{n}_id` | 框选区域 ID | 是 | P0 |
| `selection_{n}_name` | 框选区域名称 | 建议 | P1 |
| `selection_{n}_row_range` | 框选区域对应行区间，格式建议 `startRow-endRow` | 是 | P0 |
| `selection_{n}_column_range` | 框选区域对应列区间，格式建议 `startColumn-endColumn` | 是 | P0 |
| `selection_{n}_avg_pressure` | 框选区域平均压强 | 是 | P0 |
| `selection_{n}_max_pressure` | 框选区域最大压强 | 是 | P0 |
| `selection_{n}_max_pressure_coord` | 框选区域最大值坐标，坐标基于全量矩阵 | 是 | P0 |
| `selection_{n}_min_pressure_non_zero` | 框选区域非零最小压强 | 是 | P0 |
| `selection_{n}_pressure_sum` | 框选区域压力总和 | 是 | P0 |
| `selection_{n}_contact_area` | 框选区域受压面积 | 是 | P0 |
| `selection_{n}_active_sensor_count` | 框选区域有效传感点数 | 是 | P0 |
| `selection_{n}_center_of_pressure` | 框选区域压力重心坐标 | 建议 | P1 |
| `selection_{n}_data` | 框选区域矩阵或派生数据 | 建议 | P1 |

说明：框选字段是派生数据，不能替代全量 `real_data`。即使有框选，CSV 仍必须导出全量矩阵。

### 13.6.2 CSV legacy 字段映射规则

V2.0 采用推荐兼容范围：兼容无版本号、常见旧字段别名和缺省 metadata；不做完整字段映射配置器。

| legacy 字段/形态 | 标准字段 | 兼容规则 | 不兼容处理 | 优先级 |
|---|---|---|---|---|
| 无 `csv_format_version` | `csv_format_version=legacy` | 按旧格式解析 | 无法识别时提示“CSV 版本不支持” | P0 |
| `time` | `timestamp` | 作为时间戳或秒数读取 | 非数字时报错 | P0 |
| `sec(s)` | `timestamp` | 作为秒数读取，导入后可转换为毫秒 | 非数字时报错 | P1 |
| `{key}aver` | `avg_pressure` | 按 key 映射到坐垫/靠背/整体 | key 无法识别时忽略该列并提示 | P0 |
| `{key}max` | `max_pressure` | 按 key 映射 | 非数字时报错 | P0 |
| `{key}maxCoord` | `max_pressure_coord` | 按 key 映射 | 坐标无法解析时报错 | P0 |
| `{key}pressTotal` | `pressure_sum` | 按 key 映射 | 非数字时报错 | P0 |
| `{key}pressureArea` | `contact_area` | 按 key 映射 | 非数字时报错 | P0 |
| `{key}point` | `active_sensor_count` | 按 key 映射 | 非数字时报错 | P0 |
| `{key}realData` | `real_data` | 解析为全量矩阵 | 缺失时不能回放和对比 | P0 |
| `{key}selectData` | `select_data` | 作为派生区域数据导入 | 解析失败不影响全量导入 | P1 |

### 13.6.3 CSV 导入缺省策略

| 缺失字段 | 缺省处理 | 是否允许导入 | 影响 |
|---|---|---|---|
| `csv_format_version` | 标记为 legacy | 允许 | 使用 legacy 映射 |
| `software_version` | 置为空 | 允许 | 仅影响追溯 |
| `device_mac` | 置为空并提示 | 允许回放，不允许参与严格设备对比 | 影响可追溯 |
| `device_type` | 尝试从字段 key 或矩阵尺寸推断 | 不确定时禁止对比 | 影响展示和对比 |
| `sample_rate_hz` | 按时间戳估算；无法估算则默认 1fps 回放 | 允许 | 回放速度可能不准 |
| `data_direction` | 按 none/0 度处理 | 允许 | 提示 legacy 数据方向信息缺失 |
| `real_data` | 无法缺省 | 不允许 | 无全量矩阵无法回放 |

### 13.7 数据对比业务逻辑

| 项目 | 业务规则 | 当前状态 | 优先级 |
|---|---|---|---|
| 对比定位 | 历史记录 A/B 对比工具，不是实时对比工具 | 已确认 | P0 |
| 对比入口 | 从历史数据列表选择 A、B 两条记录进入 | 待补齐 | P0 |
| 对比组数 | V2.0 只支持两组 A/B，不做多组对比 | 已确认 | P0 |
| 数据来源 | V2.0 只支持历史数据，不支持实时 + 历史、不支持历史 + CSV | 已确认 | P0 |
| 对比视图 | A 图、B 图、B-A 差值图 | 待补齐 | P0 |
| 同步方式 | 按播放进度百分比对齐 A/B 帧 | 已确认 | P0 |
| 指标展示 | 显示 A、B、差值、变化率 | 待补齐 | P0 |
| 框选对比 | 一个框选区域同时作用于 A/B 和差值图 | 待补齐 | P0 |
| 方向统一 | 对比前需要统一显示方向 | 待补齐 | P0 |
| 采样率差异 | 允许对比，但提示“采样率不同，按进度对齐” | 待补齐 | P1 |
| 矩阵尺寸统一 | 不同尺寸不能直接对比，必须提示 | 待补齐 | P0 |

### 13.7.1 数据对比主流程

| 步骤 | 用户动作 | 系统处理 | 成功结果 | 异常处理 |
|---|---|---|---|---|
| 1 | 进入历史数据 | 加载历史记录列表 | 显示可选历史数据 | 加载失败提示 |
| 2 | 选择 A 记录 | 记录 A 的基础信息 | A 被选中 | 数据损坏时提示 |
| 3 | 选择 B 记录 | 记录 B 的基础信息 | B 被选中 | 数据损坏时提示 |
| 4 | 点击开始对比 | 校验 A/B 是否可比 | 进入对比模式 | 不可比时阻止进入并说明原因 |
| 5 | 查看 A/B 图 | 加载当前帧矩阵 | A/B 并排显示 | 当前帧缺失时显示空状态 |
| 6 | 查看差值图 | 计算 B-A | 显示红蓝差值图 | 无法计算时提示 |
| 7 | 拖动进度条 | 按百分比同步 A/B 帧 | 三图同步变化 | 帧数不同按百分比映射 |
| 8 | 查看指标表 | 计算 A、B、差值、变化率 | 指标表更新 | A=0 时变化率显示 N/A |
| 9 | 创建框选 | 同一坐标作用于 A/B 和差值图 | 显示局部对比指标 | 越界时不创建 |
| 10 | 退出对比 | 清理对比状态 | 返回历史数据或主界面 | 不修改历史原始数据 |

### 13.7.2 数据对比校验规则

| 校验项 | 通过条件 | 失败提示 | 优先级 |
|---|---|---|---|
| 记录选择 | A/B 均已选择，且不是同一条记录 | 请分别选择基准数据 A 和对比数据 B | P0 |
| 设备类型 | A/B 设备类型一致 | 两组数据设备类型不同，不能直接对比 | P0 |
| 矩阵尺寸 | A/B 行列一致 | 两组数据矩阵尺寸不同，不能直接对比 | P0 |
| 坐垫/靠背 | A/B 当前对象一致 | 请分别选择坐垫对坐垫、靠背对靠背 | P0 |
| 数据结构 | A/B 都包含全量矩阵 | 数据缺少全量矩阵，不能对比 | P0 |
| 帧数 | A/B 至少各有 1 帧 | 数据为空，不能对比 | P0 |
| 压力单位 | A/B 单位一致 | 两组数据单位不一致，不能直接对比 | P0 |
| 置零口径 | 建议一致 | 两组数据置零口径不同，对比结果仅供参考 | P1 |
| 翻转方向 | 可统一到当前显示方向 | 如无法统一，提示用户先调整方向 | P0 |

### 13.7.3 帧同步和差值计算规则

| 项目 | 规则 | 优先级 |
|---|---|---|
| 对齐方式 | 按播放进度百分比同步 A/B | P0 |
| 当前帧计算 | A 当前帧 = A 总帧数 x 当前进度；B 当前帧 = B 总帧数 x 当前进度 | P0 |
| 帧数不同 | 允许对比，按百分比映射到最近帧 | P0 |
| 采样率不同 | 允许对比，但提示“采样率不同，按进度对齐” | P1 |
| 单点差值 | B 点值 - A 点值 | P0 |
| 指标差值 | B 指标 - A 指标 | P0 |
| 变化率 | (B - A) / A x 100% | P0 |
| A=0 | 变化率显示 N/A，不报错 | P0 |

### 13.7.4 差值图规则

| 项目 | 规则 | 优先级 |
|---|---|---|
| 图类型 | 红蓝差值图 | P0 |
| 颜色含义 | 红色表示 B 大于 A，蓝色表示 B 小于 A，灰色/白色表示接近无变化 | P0 |
| 图例 | 必须显示颜色含义和数值范围 | P0 |
| 默认范围 | 以当前 A/B 差值最大绝对值作为上下限，保证正负对称 | P0 |
| 悬浮信息 | 鼠标悬停显示 A 值、B 值、差值、变化率 | P1 |
| 阈值过滤 | P1 支持隐藏小于阈值的变化 | P1 |

### 13.7.5 对比指标

| 指标 | V2.0 是否做 | 计算范围 | 说明 |
|---|---|---|---|
| 平均压强 | 是 | 全量/框选 | 排除 0 |
| 最大压强 | 是 | 全量/框选 | 取最大值 |
| 最小压强 | 是 | 全量/框选 | 排除 0 |
| 压力总和 | 是 | 全量/框选 | 排除 0 |
| 受压面积 | 是 | 全量/框选 | 单位 cm² |
| 有效受压点数 | 是 | 全量/框选 | 大于阈值的点数 |
| 压力重心 | 是 | 全量/框选 | 排除 0 |
| 左右均衡指数 | 否 | 全量/框选 | P1 |
| 前后均衡指数 | 否 | 全量/框选 | P1 |
| 正态分布指标 | 否 | 无 | V2.0 不做 |

### 13.7.6 数据对比待确认项和方案

| 待确认项 | 选项一简介 | 选项二简介 | 选项三简介 | 当前采用/推荐 |
|---|---|---|---|---|
| 压力单位 | 沿用当前软件单位 | 在对比页统一转换成标准单位 | 显示原单位但提示不可比 | 当前采用软件单位一致才可对比 |
| 采样率不同是否允许 | 允许，按进度百分比对齐并提示 | 禁止对比 | 自动重采样 | 当前采用允许，并提示按进度对齐 |
| 差值图默认范围 | 按当前帧最大绝对差值 | 按整段历史最大绝对差值 | 用户手动设置 | 当前采用当前帧最大绝对差值 |
| 置零口径不同是否允许 | 允许但提示仅供参考 | 禁止对比 | 自动转换 | 当前采用允许但提示仅供参考 |
| 对比导出是否进 V2.0 | 不进 P0，放 P1 | 进 P0 | 不做 | 当前采用不进 V2.0 P0，放 P1 |
| 对比曲线是否进 V2.0 | 不进 P0，放 P1 | 进 P0 | 不做 | 当前采用不进 V2.0 P0，放 P1 |

### 13.7.7 对比结果导出规则（P1）

V2.1 默认采用轻量导出范围：只导出当前帧和框选指标，不导出全帧序列和完整差值矩阵。

| 项目 | 规则 | 优先级 |
|---|---|---|
| 导出入口 | 数据对比页面提供“导出对比结果”按钮 | P1 |
| 导出前置条件 | A/B 已通过可比性校验，当前对比结果可正常显示 | P1 |
| 文件格式 | CSV | P1 |
| 文件命名 | `对比结果_A记录名_B记录名_导出时间.csv` | P1 |
| 导出范围 | 默认导出当前帧 A/B/差值指标；后续可导出全帧序列 | P1 |
| 导出字段 | A 基础信息、B 基础信息、frame_index、A 指标、B 指标、差值、变化率 | P1 |
| 框选导出 | 如有框选，导出每个框选区域的 A/B/差值指标 | P1 |
| 差值矩阵 | 默认不导出完整差值矩阵；如需要，作为 P2 增强 | P2 |
| 导出失败 | 提示失败原因，保留当前对比页面 | P1 |

### 13.7.8 对比曲线规则（P1）

| 曲线 | 数据来源 | 计算方式 | 展示规则 | 优先级 |
|---|---|---|---|---|
| A 压力总和曲线 | A 全帧或框选帧 | 每帧压力总和 | 与 B 曲线同图展示 | P1 |
| B 压力总和曲线 | B 全帧或框选帧 | 每帧压力总和 | 与 A 曲线同图展示 | P1 |
| 压力总和差值曲线 | A/B 对齐帧 | B - A | 可单独显示或叠加显示 | P1 |
| A/B 面积曲线 | A/B 全帧或框选帧 | 每帧受压面积 | 同图展示 | P1 |
| 面积差值曲线 | A/B 对齐帧 | B 面积 - A 面积 | 同步进度条 | P1 |
| 重心轨迹对比 | A/B 每帧重心 | 按帧绘制轨迹 | 颜色区分 A/B | P2 |

说明：对比曲线不进入 V2.0 P0。V2.0 先保证 A/B 三图、当前帧指标和差值正确，P1 再补曲线。

### 13.8 待补齐项

| 编号 | 问题 | 优先级 | 处理建议 |
|---|---|---|---|
| 导出-01 | CSV 缺少格式版本 | P0 | 增加 `csv_format_version` |
| 导出-02 | CSV 字段和 PRD 字段不一致 | P0 | 建字段映射表 |
| 导出-03 | `/downlaod` 拼写问题 | P2 | 保留旧接口，新增 `/download` |
| 导出-04 | CSV 导入版本兼容不清 | P1 | V1.3 已补 legacy 字段映射和缺省策略 |
| 导出-05 | 数据对比模块逻辑不完整 | P1 | V1.3 已补 P1 对比导出和对比曲线规则 |

### 13.9 验收标准

| 编号 | 验收项 | 通过标准 | 优先级 |
|---|---|---|---|
| 验收-导出-01 | CSV 导出 | 可从历史数据导出 CSV | P0 |
| 验收-导出-02 | 路径选择 | 可选择保存路径 | P0 |
| 验收-导出-03 | 导出成功 | 可打开文件或文件夹 | P0 |
| 验收-导出-04 | CSV metadata | 包含设备、采样率、置零、方向等信息 | P0 |
| 验收-导出-05 | CSV 导入 | 能校验结构并加载 | P1 |
| 验收-导出-06 | 错误提示 | 结构不符时提示“数据有误” | P0 |


### 13.10 PRD1.1 增量补充

#### 13.10.1 CSV 单文件导出

| 项 | PRD1.1 规则 |
|---|---|
| 文件数量 | 同一条历史记录只导出一个 CSV 文件 |
| 多矩阵数据 | 坐垫和靠背按行合并，通过 `matrix_key/device_type` 区分 |
| 标准字段 | 包含 `software_version`、`device_mac`、`device_type`、`frame_index`、`timestamp`、`pressure_unit`、`noise_removed`、`data_direction` |
| 方向字段 | 导出 `rotate_degree`、`data_direction` 等方向信息 |
| 置零字段 | 导出 `zero_enabled`、`zero_time`、`zero_state` |
| 框选字段 | 有框选时导出 `selection_1_*` 等框选派生字段 |
| carY 字段 | carY 导出增加 `pressure_conversion=carY_100_div_3` 和说明字段 |

#### 13.10.2 CSV 导入校验

| 项 | PRD1.1 规则 |
|---|---|
| 结构校验 | 导入前校验表头、矩阵列、统计列和矩阵长度 |
| 支持矩阵 | 支持 32 x 32、46 x 46、50 x 64、4096 等软件认可矩阵 |
| 数值校验 | 矩阵元素必须为数字 |
| 错误提示 | 校验失败提示“数据有误”，并删除临时上传文件 |
| 列表去重 | 导入列表按路径去重，上传/删除/切换页签后清空操作态 |
| 导入回放 | 导入成功后可以直接播放，不出现 message error |
| legacy 兼容 | 兼容旧版 `xxx Data/Max/Aver/Area` 表头和新版 `realData` 表头 |

#### 13.10.3 数据对比新版 UI

| 区域 | PRD1.1 规则 |
|---|---|
| header | 对比页复用主页一级 header，隐藏二级工具栏和普通分析面板 |
| 主热力图 | 采用 A 基础数据、B-A 差值图、B 对比数据三栏 |
| 热力数字图 | A/B 图使用靠背/座椅 2D 数字同款视觉，差值图使用红蓝语义 |
| 放大查看 | 每张热力图右上角放大 icon 可打开更清晰的大图 |
| 5x5 放大镜 | 鼠标悬停显示局部 5x5 矩阵，遵守靠背有效区隐藏规则 |
| 播放栏放大 | 点击播放栏放大 icon 后，同时放大三张热力图和进度条 |
| 对象选择 | 对比对象下拉框有蓝色高亮边框和箭头，明确可交互 |
| 控件样式 | 按钮、下拉框、输入框统一暗色样式，不使用白色控件 |
| 右侧面板 | 按区域管理、模板、导出预览分组 |

#### 13.10.4 数据对比模式

| 模式 | PRD1.1 规则 |
|---|---|
| 跨记录对比 | 从两条不同历史记录选择 A/B，对共同矩阵 key 进行对比 |
| 同记录时间点对比 | 同一条历史记录内选择 A/B 两个时间点进行对比 |
| 同帧限制 | 同记录时间点对比禁止 A/B 选择同一帧 |
| 采样率不同 | 允许对比，但状态栏需展示 A/B 采样率和对齐方式 |
| 对象校验 | 坐垫只能对坐垫，靠背只能对靠背 |
| 错误提示 | 不可比时提示“请分别选择坐垫对坐垫、靠背对靠背。” |

#### 13.10.5 对比曲线和汇总表

| 项 | PRD1.1 规则 |
|---|---|
| 跨记录曲线 | 压力/面积曲线展示 A、B、B-A 三条曲线 |
| 同记录曲线 | 展示单条趋势线，并用 A/B 垂直标记标出两个时间点 |
| 坐标信息 | 曲线必须有横纵坐标、刻度、单位和图例说明 |
| 汇总表 | 展示 A、B、B-A、变化率 |
| 颜色箭头 | 差值和变化率用红/绿/灰区分，并显示上升、下降、持平箭头 |
| 导出 | 支持导出当前对象、范围、A 指标、B 指标、B-A、变化率 |

#### 13.10.6 PRD1.1 验收标准

| 编号 | 验收项 | 通过标准 |
|---|---|---|
| CSV-1.1-01 | 单文件导出 | 同一记录只导出一个 CSV，坐垫/靠背可区分 |
| CSV-1.1-02 | 导入校验 | 错误 CSV 提示“数据有误”，不进入列表 |
| CSV-1.1-03 | 导入播放 | 导入成功后可以直接播放 |
| CMP-1.1-01 | 三图对比 | A、B-A、B 三张热力数字图显示正确 |
| CMP-1.1-02 | 热力图放大 | 每张热力图可以打开清晰大图 |
| CMP-1.1-03 | 5x5 放大镜 | 鼠标悬停显示局部 5x5 矩阵 |
| CMP-1.1-04 | 播放栏放大 | 弹窗中三图和进度条同步刷新 |
| CMP-1.1-05 | 曲线和汇总 | 曲线有坐标单位，汇总表有差值颜色和变化箭头 |

## 14. 全局数据规则

### 14.1 数据分层

| 数据层 | 定义 | 是否必须保留 | 影响因素 |
|---|---|---|---|
| 设备原始输入 | rawFrame/rawPointArray，设备原始帧和一维点数组 | 必须保留 | 不受任何工具影响，用于排查 |
| 业务原始矩阵 | rawData，软件全链路使用的基准压力矩阵 | 必须保留 | 受翻转/旋转影响，不受颜色、润滑、噪点消除、框选影响 |
| 后端处理数据 | backendProcessedData，后端在 rawData 基础上处理后的矩阵 | 按处理状态生成 | 受噪点消除影响 |
| 置零后数据 | 扣除置零基线后的矩阵 | 根据置零状态生成 | 受置零影响 |
| 显示数据 | 用于界面展示的矩阵 | 必须可重建 | 受颜色、润滑、高度、响应速度影响 |
| 统计数据 | 用于计算指标的矩阵 | 口径必须明确 | 受置零、框选、噪点消除影响 |
| 入库数据 | 采集保存到数据库的数据 | 必须可追溯 | 方向和置零口径需明确 |
| 导出数据 | CSV 导出的数据 | 必须标明口径 | 需和导入兼容 |

### 14.2 功能对数据层影响

| 功能 | 原始数据 | 显示数据 | 统计数据 | 入库数据 | 导出数据 | 结论 |
|---|---|---|---|---|---|---|
| 翻转/旋转 | rawFrame 不影响；业务 rawData 按修正方向重建 | 影响 | 影响坐标、框选、重心 | 保存修正后业务矩阵和方向 metadata | 导出修正后业务矩阵和方向 metadata | V2.0 已确认 |
| 置零 | 不影响设备原始帧 | 影响 | 影响 | 保存置零后业务矩阵和置零 metadata | 导出置零后业务矩阵和置零 metadata | V2.0 已确认 |
| 颜色调节 | 不影响 | 影响 | 不影响 | 不影响 | 不影响 | 清晰 |
| 高斯润滑 | 不影响 | 影响 | V2.0 不影响 | 不影响 | 不影响 | V2.0 只做显示 |
| 噪点消除 | 不影响 rawData，生成 backendProcessedData | 影响 | 影响 | 保存后端处理后的业务矩阵和处理标记 | 导出后端处理后的业务矩阵和处理标记 | V2.0 已确认 |
| 框选 | 不影响 | 影响局部显示 | 影响统计范围 | 保存全量矩阵 + 框选元数据 | 可导出派生数据 | V2.0 已确认 |

### 14.3 原始数据原则

| 编号 | 原则 | 优先级 |
|---|---|---|
| 原则-01 | 框选不得导致全量原始矩阵丢失 | P0 |
| 原则-02 | 颜色、高斯润滑、高度、响应速度不得污染 rawData | P0 |
| 原则-03 | 翻转/旋转会修正业务 rawData，必须保存方向并保证可还原 | P0 |
| 原则-04 | 置零如影响统计或导出，必须记录置零状态和置零时间 | P0 |
| 原则-05 | CSV 导出必须支持后续导入、回放和对比 | P0 |
| 原则-06 | 噪点消除不覆盖 rawData，但会生成后端处理数据流并影响统计、采集和导出 | P0 |
| 原则-07 | 任何导入的 legacy 数据如果缺失关键 metadata，必须提示用户其可比性受限 | P1 |


### 14.4 PRD1.1 数据口径补充

| 数据口径 | PRD1.1 规则 |
|---|---|
| 方向 | 翻转和旋转改变业务数据源并持久化，采集、回放、导出按方向 metadata 处理 |
| 置零 | 置零进入后端采集和导出口径，不只是显示效果 |
| 框选 | 框选只影响显示、统计、图表和对比范围，不影响采集全量数据 |
| 颜色 | 颜色、润滑、高度只影响显示，不影响 rawData、采集、导出 |
| 有效 mask | endi 靠背有效区 mask 被 2D、3D、原始数据、对比页共用 |
| 异常帧 | 异常帧不得污染采集、历史、回放、导出和对比 |

## 15. 发布前 P0 清单

| 编号 | 模块 | P0 问题 | 业务影响 | 负责人 | 状态 | 备注 |
|---|---|---|---|---|---|---|
| P0-01 | 连接 | 设备类型识别口径统一 | 影响设备能否正确采集 | 待补充 | 未开始 | 固件返回 vs MAC 映射 |
| P0-02 | 连接 | 授权失败禁止采集 | 影响设备安全和交付 | 待补充 | PRD 已补充 | `/startCol` 强校验已补 |
| P0-03 | 连接 | 设备参数字段补齐 | 影响历史追溯和 CSV | 待补充 | PRD 已补充 | 固件版本由硬件返回；压力单位暂用软件内部单位 |
| P0-03A | 连接 | 一键连接全流程超时和连接锁 | 防止连接卡死导致软件不可用 | 待补充 | PRD 已补充 | 连接中/重连中禁止重复触发 |
| P0-03B | 连接 | 重连前资源释放 | 防止旧串口、parser、listener、timer 残留 | 待补充 | PRD 已补充 | `/rescanPort` 前清理 |
| P0-04 | 数据处理 | 原始帧、原始矩阵、显示矩阵字段统一 | 影响全链路数据可信度 | 待补充 | PRD 已补充 | 已补标准数据字典 |
| P0-05 | 数据处理 | 坏帧、断帧、长度不匹配处理规则补齐 | 影响实时展示和采集稳定性 | 待补充 | PRD 已补充 | 已补异常帧阈值和提示规则 |
| P0-06 | 数据处理 | 设备类型到矩阵尺寸映射补齐 | 影响展示、统计和采集 | 待补充 | 未开始 | 32x32、46x46、50x64、64x64 |
| P0-07 | 展示 | 缩放范围确认 | 影响用户查看细节 | 待补充 | PRD 已补充 | V2.0 确认为 10%-300% |
| P0-08 | 展示 | 当前无 2D 热力图，需从 V2.0 范围移除 | 避免研发误做或 QA 误测 | 待补充 | 未开始 | 后续如做，单独定义颜色映射和图例 |
| P0-09 | 工具 | 置零数据流补齐 | 影响数据可信度 | 待补充 | PRD 已补充 | 显示/统计/采集/导出 |
| P0-10 | 工具 | 框选和采集解耦 | 防止框选误控制采集范围 | 待补充 | PRD 已补充 | 采集始终保存全量矩阵 |
| P0-10A | 工具 | 框选统计口径确认 | 影响区域分析可信度 | 待补充 | PRD 已补充 | 平均/最小/总和/重心排除 0 |
| P0-11 | 工具 | 翻转/旋转方向修正持久化 | 影响展示、统计、采集、回放和 CSV | 待补充 | 未开始 | 坐垫/靠背独立，支持 0/90/180/270 |
| P0-11A | 工具 | 采集中禁止翻转/旋转 | 防止采集中数据方向口径变化 | 待补充 | 未开始 | 停止采集后再修改方向 |
| P0-11B | 工具 | 多量尺上限和计算规则 | 影响测距可信度和交互稳定 | 待补充 | PRD 已补充 | 最多 8 条，点距由软件按设备类型配置 |
| P0-12 | 数据 | 统计公式和单位补齐 | QA 无法验收 | 待补充 | PRD 已补充 | 数据字典 |
| P0-13 | 数据 | carY 分压规则解释 | 客户无法理解数据 | 待补充 | PRD 已补充 | `100 / 3` |
| P0-14 | 采集 | 历史字段结构补齐 | 影响追溯 | 待补充 | PRD 已补充 | MAC、清零、方向 |
| P0-15 | 采集 | 异常恢复流程补齐 | 影响数据防丢失 | 待补充 | PRD 已补充 | 强退恢复 |
| P0-16 | 导出 | CSV 格式版本补齐 | 影响导入和兼容 | 待补充 | PRD 已补充 | `csv_format_version` |
| P0-17 | 导出 | CSV 字段映射补齐 | 影响科研分析 | 待补充 | PRD 已补充 | 当前字段 vs 标准字段 |
| P0-18 | 数据对比 | 历史 A/B 对比校验 | 防止不可比数据进入对比 | 待补充 | 未开始 | 类型、矩阵、坐垫/靠背、全量矩阵 |
| P0-19 | 数据对比 | A/B/差值图和指标差值 | 形成数据对比核心闭环 | 待补充 | 未开始 | B-A、变化率、A=0 显示 N/A |

## 16. 发布准入

| 检查项 | 准入标准 | 是否阻塞 |
|---|---|---|
| 设备连接 | 真实设备一键连接成功率满足现场使用 | 是 |
| 授权校验 | 非法设备不能采集 | 是 |
| 类型校验 | 设备类型不匹配不能采集 | 是 |
| 实时展示 | 2D/3D 视图可实时反映压力变化 | 是 |
| 数据采集 | 连续采集 30 分钟无明显卡死或数据中断 | 是 |
| 历史数据 | 停止采集后历史记录不丢失 | 是 |
| 回放 | 历史数据可完整回放，拖动进度不崩溃 | 是 |
| CSV | 导出 CSV 可打开、可导入、字段可解释 | 是 |
| 置零 | 置零影响数据基线，不只是按钮状态变化 | 是 |
| 框选 | 框选不控制采集范围，采集始终保存全量矩阵 | 是 |
| 框选模板 | 模板只保存区域配置，不保存压力数据，不影响采集 | 否 |
| 翻转/旋转 | 方向修正可持久化，坐垫/靠背独立，采集中禁止修改 | 是 |
| 量尺 | 按传感点距计算，最多 8 条，缩放/翻转不影响真实距离 | 否 |
| 数据对比 | 可比数据才能进入 A/B 对比，差值和变化率计算正确 | 是 |
| 异常恢复 | 采集中强退后可恢复或保留数据 | 是 |
| 安装包 | Windows/macOS 打包后可启动，缓存路径正确 | 是 |

## 17. 后续版本规划

| 方向 | 功能 | 建议版本 | 说明 |
|---|---|---|---|
| 数据对比 | 历史 A/B 数据对比 | V2.0 | 两条历史记录、A/B/差值图、指标差值 |
| 数据对比 | 对比 CSV 导出 | V2.1 | 导出 A/B/差值/变化率 |
| 数据对比 | 对比曲线 | V2.1 | 压力总和、面积、重心轨迹 |
| 数据对比 | 历史数据和导入 CSV 对比 | V2.1 | 外部 CSV 参与对比 |
| 框选 | 框选模板 | V2.1 | 保存和应用常用分析区域 |
| 报告能力 | PDF/Word 报告 | V2.2 | 面向科研和客户交付 |
| 3D 增强 | 3D 模型上传 | V2.2 | 支持客户模型 |
| 交付增强 | 多语言切换 | V2.2 | 中文/英文 |
| 交付增强 | macOS 签名和公证 | V2.2 | 正式交付需要 |

## 18. 关联文档

| 文档 | 说明 |
|---|---|
| `压力感知软件产品需求文档-飞书表格版-含数据处理模块.md` | 当前文档，新增数据处理模块后的优化版 |
| `压力感知软件产品需求文档-飞书表格版-含业务逻辑.md` | 上一版主文档 |
| `PRD-JQ-Tools-V2-飞书表格版.md` | 旧飞书表格版 |
| `PRD-JQ-Tools-V2-飞书粘贴版.md` | 飞书段落版 |
| `PRD-JQ-Tools-V2-Implementation-Aligned.md` | 当前实现对齐版 PRD |
| `shroom/FUNCTION_IMPLEMENTATION.md` | 当前实现逻辑说明 |
| `opt.md` | 实现文档优化标注 |
| `shroom/一键连接与重新连接异常处理产品需求文档.md` | 已整合到本 PRD 连接模块，作为连接异常细化来源 |
| `shroom/框选功能产品需求文档.md` | 已整合到本 PRD 工具模块，作为框选和模板细化来源 |
| `shroom/数据对比功能产品需求文档.md` | 已整合到本 PRD 导出与数据对比模块，作为数据对比细化来源 |
| `shroom/数据对比功能调研.md` | 数据对比竞品和方案调研来源 |
| `shroom/量尺与画布翻转产品需求文档.md` | 已整合到本 PRD 工具模块，作为量尺、翻转、旋转和方向持久化细化来源 |
| `shroom/noCompleted.md` | 功能完成度跟踪来源，本次 V1.3 根据该文档补齐 PRD 规则 |
| `shroom/dec.md` | V1.3 未能直接定稿、需要你确认的产品/硬件/算法决策项 |


## 19. PRD1.1 增量发布准入

| 编号 | 准入项 | 是否阻塞 |
|---|---|---|
| REL-1.1-01 | 真实设备一键连接、重连、断开流程稳定 | 是 |
| REL-1.1-02 | MAC/Unique ID 强校验和批量保存稳定 | 是 |
| REL-1.1-03 | 采集 30 分钟无明显数据中断、无 UI 卡死 | 是 |
| REL-1.1-04 | 坐垫/靠背 2D、3D、整体视图切换稳定 | 是 |
| REL-1.1-05 | 框选不影响采集全量数据 | 是 |
| REL-1.1-06 | 回放拖动进度后框选数据仍正确显示 | 是 |
| REL-1.1-07 | CSV 单文件导出、可导入、可回放 | 是 |
| REL-1.1-08 | 数据对比 A/B/B-A 三图、曲线、汇总表正确 | 是 |
| REL-1.1-09 | 对比播放放大弹窗可放大三图和进度条 | 是 |
| REL-1.1-10 | 打包后缓存写入路径正确，无 `app.asar` 写入错误 | 是 |
| REL-1.1-11 | 异常帧、坏 CSV、非法倍速均有兜底处理 | 是 |
| REL-1.1-12 | 置零、方向、框选、CSV metadata 口径可追溯 | 是 |

## 20. PRD1.1 待确认事项

| 编号 | 问题 | 当前建议 |
|---|---|---|
| TBD-1.1-01 | 压力单位是否统一换算为标准 kPa | 当前沿用软件内部单位，界面显示 kPa 文案需再确认 |
| TBD-1.1-02 | carY 分压规则是否对外展示 | 建议在数据字典和导出说明中明确 `100 / 3` 规则 |
| TBD-1.1-03 | 强退采集恢复流程 | 需要确认“恢复、丢弃、另存为异常记录”的最终交互 |
| TBD-1.1-04 | CSV legacy 兼容范围 | 需要列出所有历史字段别名和缺省策略 |
| TBD-1.1-05 | 对比报告导出 | 当前只做 CSV；PDF/Word 报告放 V2.2 |
| TBD-1.1-06 | 置零是否永久影响采集数据 | 当前按后端采集口径处理；若业务只想影响显示，需要重新确认 |

## 21. PRD1.1 关联增量文档

| 文档 | 说明 |
|---|---|
| `PRD/PRD1_1_增量清单.md` | 使用 release-notes skill 整理的近期新增、优化、修复和破坏性变更 |
| `ARCHITECTURE.md` | 每次代码更新后的架构和实现更新记录 |
| `PRD/框选功能产品需求文档.md` | 框选功能专项需求 |
| `PRD/数据对比功能产品需求文档.md` | 数据对比专项需求 |
| `PRD/数据对比功能调研-时间维度扩展-brainstorm-ideas版.md` | 数据对比时间维度扩展参考 |
