# ─── 端口配置说明 ────────────────────────────────────────
# 以下环境变量会在 Electron 主进程启动 React dev server 时自动设置：
#   REACT_APP_API_PORT  — 后端 API 端口（默认 19245，端口冲突时自动分配）
#   REACT_APP_WS_PORT   — WebSocket 端口（默认 19999，端口冲突时自动分配）
#
# 如果需要手动指定端口（不通过 Electron 启动），可以取消下面的注释：
# REACT_APP_API_PORT=19245
# REACT_APP_WS_PORT=19999

# 远程服务器地址
REACT_APP_SERVER_ADDRESS=https://sensor.bodyta.com
